# SPEC-AI-004 수동 검증 기록 — 프롬프트 핫픽스 실 CLI 재검증 (T4)

- 검증일: 2026-07-17
- 모델: `haiku` (`resolveModel(false)` 기본값) / claude CLI 2.1.212
- 브랜치: feature/SPEC-AI-004 (핫픽스된 `src-tauri/src/ai/prompt.rs` HEAD 기준)
- 실행 방식: SPEC-AI-003 방법론 재사용. `prompt.rs` 조립 로직을 코드 그대로 충실 재현 — `COMMON_INSTRUCTION` 결합, `INLINE_CONTEXT_GUARD` 조건 삽입(문맥 구획 ≥1일 때만 user-prompt 선두), Continue base 신규 문구(D-B 재복창 금지 + D-D 온건 분량·형식 상한), Diagram 신규 문구(D-C 양성 예시), `continue_system_prompt(has_after)` 분기 — 한 뒤, `claude_cli.rs::build_claude_args`와 동일 인자로 실 CLI 실행:
  `claude -p <user> --system-prompt <sys> --model haiku --output-format stream-json --include-partial-messages --verbose --setting-sources ""` + `MAX_THINKING_TOKENS=0`, cwd=빈 스크래치, stdin null. `type:"result"`의 `result` 필드를 최종 출력으로 채택(릴레이 로직과 동일).
- 입력 문서: SPEC-AI-003 검증에 사용한 대표 문서 3종·시나리오 입력 원문을 세션 스크래치(`scratchpad/ai-sim/`)에서 **그대로 재사용**(비교 유효성 확보, 재구성 없음). 이어쓰기 계열(s01/s02/s03/s04/s11)은 user-prompt 무변경 + system-prompt만 신규, 인라인 계열(s06/s07/s08/s09/s10)은 기존 user-prompt 선두에 가드 삽입. **s04는 `[뒤 문맥]` 섹션 없음 + base system-prompt만(하위호환) 확인 완료.**
- 호출 결과: 20/20 성공, 재시도 0회, UNVERIFIED 0건. 평균 응답 4.3초.

## 사용한 최종 지시문 3종 원문 (prompt.rs HEAD)

### 1. 인라인 문맥 가드 (`INLINE_CONTEXT_GUARD`, D-A) — 문맥 구획 ≥1일 때 user-prompt 선두 삽입

```
[앞 문맥]과 [뒤 문맥]은 참고용일 뿐이며, [대상]만 변환하고 문맥의 내용은 결과에 포함하지 말라.
```

### 2. 이어쓰기 base (`AiFeature::Continue.system_prompt()`, D-B + D-D) — has_after 양쪽 공통 상속

```
너는 문서 작성 보조자다. 문서의 어조와 종결어미를 그대로 이어받아 직전 본문에 자연스럽게 이어지는 다음 내용을 작성하라. 문서 개요와 직전 본문의 맥락에서 벗어나지 말라. 이미 작성된 직전 본문을 다시 출력하거나 반복하지 말고, 끊긴 지점 바로 다음부터 새 텍스트만 이어서 작성하라. 분량은 한두 문단 이내로 하고, 직전 본문에 없던 코드 블록·표·목차 같은 새로운 형식을 임의로 도입하지 말라.

결과 텍스트만 출력하라. 설명·인사·사족을 붙이지 말라. 마크다운 코드펜스는 요청받은 경우에만 사용하라.
```

has_after=true(s01/s02/s03/s11) 시 아래를 추가 결합(SPEC-AI-003 조건부 지시, 무변경):

```
끊긴 문장부터 이어서 완성하고, 뒤 문맥으로 자연스럽게 연결하라. 뒤 문맥의 내용을 반복하거나 선점하는 것은 금지한다.
```

### 3. 다이어그램 (`AiFeature::Diagram.system_prompt()`, D-C 양성 예시 추가)

```
주어진 절차·관계 설명을 mermaid 다이어그램으로 변환하라. 순수 mermaid 문법 코드만 출력하고, ```mermaid 코드펜스나 다른 설명 문구 없이 다이어그램 코드만 그대로 출력하라. 출력은 graph·flowchart·sequenceDiagram 등 mermaid 키워드로 시작해야 하며, 백틱 문자는 한 글자도 포함하지 말라.

결과 텍스트만 출력하라. 설명·인사·사족을 붙이지 말라. 마크다운 코드펜스는 요청받은 경우에만 사용하라.
```

## 결함 5종 × 3회 — 회차별 판정 (AC-AI4-009)

판정은 acceptance.md 고정 표의 기계적 기준으로 수행(출력 문자열 결정적 검사).

| # | 결함 | 기준 | r1 | r2 | r3 | 재현 | 인용 증거 |
|---|---|---|---|---|---|---|---|
| s07 | D-A(치명) shorten 인라인 | 출력에 `[앞/뒤 문맥]` 문장("# mdedit 플러그인 개발 가이드", "## 개요", "Node.js 20", "Playwright" 등) 미포함 | 통과 | 통과 | 통과 | **0/3** | r1: `"확장의 상태는 StateField로 선언하고, StateEffect를 통해서만 변경한다. …"` — [대상] 문단만 축약, 문맥 마커 검출 0건(3회 전부). SPEC-AI-003의 "문서 전체 요약본" 결함 소멸. |
| s09 | D-A(치명) outline 인라인 | 출력이 [대상] 범위 밖 문맥 항목(개발 환경·테스트) 미포함 | 통과 | 통과 | 통과 | **0/3** | r1: `"• 확장의 상태를 StateField로 선언 …"` — 상태 관리 규칙 항목만 개조식화. Node.js/pnpm/vitest/Playwright 항목 검출 0건(3회 전부). |
| s10 | D-C(품질) diagram 인라인 | 첫 토큰 mermaid 키워드 + 백틱 0개 + 서두 사족 없음 (≤1/3 허용) | 통과 | 통과 | 통과 | **0/3** | 3회 전부 첫 토큰 `graph`, 백틱 문자 0개, 사족 0건 → 자동 재요청 트리거 0회. SPEC-AI-003의 펜스+사족 이중 위반 소멸. **stripMermaidFence 수기 대입: 백틱 출력이 0/3으로 재현되지 않아 대입 불요**(펜스 재발 시 검증 조건이 성립하지 않음 — 일반화 정규식 자체는 AC-AI4-007 자동 게이트에서 검증). |
| s11 | D-B(치명) continue 리스트 | 출력 첫머리가 직전 본문 마지막 어절("담당은"/"온보딩 개편 시안 확정 담당은") 재반복 없음 | 통과 | 통과 | 통과 | **0/3** | 직전 본문 끝: `- 온보딩 개편 시안 확정 담당은` / 출력 첫머리 r1: `"이디자이너로 하고, …"`, r2: `"이디자이너가 담당하며, …"`, r3: `"이디자이너로 확정"` — 3회 전부 끊긴 지점 바로 다음부터 시작. SPEC-AI-003의 어절 중복 삽입 결함 소멸. |
| s02 | D-D(품질) continue 자유 위치 | 생성 ≤2문단 + 미요청 코드펜스 0개 (≤1/3 허용) | 통과(1문단) | 통과(2문단) | 통과(1문단) | **0/3** | r1: `"src/components/editor/index.ts의 extensions 배열에 추가한다. …"` — 3회 전부 코드펜스 0개, 문단 수 1·2·1. SPEC-AI-003의 ```typescript 펜스 + 추가 문단 과잉 생성 결함 소멸. |

## 통과 5종 × 1회 — 교차 오염 회귀 판정 (AC-AI4-010)

SPEC-AI-003 기록 점수(연결/뒤문맥/문체/분량/형식) 대비 퇴행 여부.

| # | 시나리오 | 기존 점수 | 판정 | 관찰 |
|---|---|---|---|---|
| s01 | continue 자유 위치(에세이) | 5/5/5/4/5 | **퇴행 없음** | "나는 문득" → `"내 숨이 하얀 연기가 되어 사라지는 것을 바라봤다. …"` 자연 완성, 2문단(신규 상한 내). 뒤 섹션(빵집) 문면 반복·선점 0건 — 마지막 문장의 "낡은 간판" 언급은 뒤 문맥으로의 연결 다리이지 문면 반복 아님(빵집/할머니/식빵 미검출). |
| s03 | continue(회의록) | 5/5/5/5/5 | **퇴행 없음** | `"사용자 테스트 없이 진행하기엔 리스크가 크다는 우려를 제기했다. …"` — 최QA 우려+합의 완성, 뒤 섹션(요금제) 미선점("요금제" 미검출), 1문단. |
| s04 | continue 문서 끝(빈 after) | 5/–/5/5/5 | **퇴행 없음(하위호환 유지)** | 프롬프트에 `[뒤 문맥]` 섹션·뒤 문맥 지시 없음(base만) 확인. `"…나는 천천히 집의 현관에 닿았다. …다음주 일요일을 이미 기다리고 있었다."` — 자연 마무리 1문단, 신규 base 지시(재복창 금지·분량 상한)가 문서 끝 경로 품질을 해치지 않음. |
| s06 | polish 인라인(무과교정) | 5/5/5/5/5 | **퇴행 없음** | 백틱 4자(인라인 코드 스팬 2곳: `` `pnpm install` ``/`` `pnpm dev` ``)를 제외하면 **원문과 문자 단위 동일**. 의미·정보 완전 보존, 문맥 흡수 0건 — 가드 삽입이 과교정을 유발하지 않음. 인라인 코드 표기 추가는 경미한 표기 변화(코드펜스 아님, 하단 특이사항 참조). |
| s08 | table 인라인 | 5/5/5/5/5 | **퇴행 없음** | 4행 마크다운 표 + 헤더·구분선 정확, 문맥 흡수 0건(StateField/CodeMirror/Node.js 미검출) — 가드가 표 변환을 방해하지 않음. |

## D2 종합 판정

| 기준 | 요구 | 결과 | 판정 |
|---|---|---|---|
| 치명 결함 결정론 (D-A: s07·s09, D-B: s11) | 각 3회 중 0회 재현 | s07 0/3, s09 0/3, s11 0/3 | **충족** |
| 품질 결함 확률 (D-C: s10, D-D: s02) | 각 3회 중 ≤1회 | s10 0/3, s02 0/3 | **충족** |
| 교차 오염(통과 5종 퇴행) | 점수 퇴행 없음 — 특히 s06 무과교정·s04 하위호환·s08 표 유지 | 5/5 시나리오 퇴행 없음 | **충족** |

**D2 최종: PASS** — AC-AI4-009 **PASS**, AC-AI4-010 **PASS**.

## 특이사항

1. **s10.r3 문맥 노드 점선 부착(판정 기준 외 관찰)**: 3회차 출력이 D-C 기준(mermaid 키워드 시작·백틱 0·사족 0)은 전부 충족했으나, 점선 노드(`I["상태 관리 StateField 선언"]` 등)로 [뒤 문맥]의 StateField/StateEffect/vitest/Playwright를 흡수(1/3). s10의 고정 표 판정 기준은 D-C뿐이므로 PASS에 영향 없으나, 인라인 diagram의 문맥 흡수(D-A 인접)가 가드에도 확률적으로 잔존함을 기록한다. r1/r2는 [대상] 절차만 정확히 도식화.
2. **s06 인라인 코드 스팬 추가**: polish가 명령어 2곳에 백틱 인라인 코드를 부착. 코드"펜스"가 아니고(COMMON_INSTRUCTION 위반 아님) 의미 무손실이므로 무과교정 판정 유지하되, 원문 완전 동일은 아님을 기록한다.
3. **stripMermaidFence 수기 대입 미실시**: 판정 조건("백틱 재현 시")이 성립하지 않음(s10 백틱 0/3). 일반화 정규식은 AC-AI4-007 자동 게이트(vitest)가 커버.
4. **입력 재사용**: 3개 대표 문서·10개 시나리오 입력 전부 SPEC-AI-003 원본을 바이트 그대로 재사용(재구성 이탈 0건) — 점수 비교 유효.

## Raw 데이터 보존 위치 (세션 스크래치)

- 조립 입력: `scratchpad/ai-sim-004/inputs/s{01,02,03,04,06,07,08,09,10,11}.{system,user}.txt`
- 출력: `scratchpad/ai-sim-004/outputs/s{07,09,10,11,02}.r{1,2,3}.{raw.json,result.txt,stderr.log}` + `s{01,03,04,06,08}.r1.*`
- 실행 스크립트: `scratchpad/ai-sim-004/run.py`
- 원본 문서·SPEC-AI-003 입력: `scratchpad/ai-sim/` (doc-a-essay.md, doc-b-tech.md, doc-c-minutes.md, outputs/)
- 전체 경로 접두: `/private/tmp/claude-501/-Users-byunjungwon-Dev-my-project-01-markdown-editor-rust/63aa2d3a-4b52-4446-b431-6803f4a73b61/`
