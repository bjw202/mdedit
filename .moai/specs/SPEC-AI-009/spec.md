---
id: SPEC-AI-009
version: "0.0.7"
status: draft
created: "2026-07-24"
updated: "2026-07-25"
author: "jw"
priority: high
issue_number: 0
dependencies:
  - SPEC-AI-001
  - SPEC-AI-003
  - SPEC-AI-006
  - SPEC-AI-008
tags:
  - ai
  - provider
  - codex
  - cli
  - integration
  - abstraction
lifecycle: spec-anchored
---

## HISTORY

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.0.1 | 2026-07-24 | jw | 최초 SPEC 작성 — `codex` CLI(0.144.1 검증)를 두 번째 AI 프로바이더로 `ProviderRegistry`에 통합. `provider.rs`의 `AiProvider` trait은 본 확장을 전제로 설계됐다(provider.rs:5-6 "M4 codex 도입 시 어댑터만 추가하면 되도록 trait 계약 확정"). 6개 모듈로 전개: (M1) provider 추상화 확장 — registry 다중 프로바이더 + 자동 감지 우선순위(claude>codex) + `providerId` 수동 오버라이드(기존 `AiRequestArgs.provider_id` 재사용, 신규 IPC 필드 0건); (M2) codex 인자 조립 — `build_codex_args`(순수)가 `exec -C <scratch> --ignore-user-config --skip-git-repo-check --ephemeral --sandbox read-only --model <model> -c 'model_reasoning_effort="medium"' --json "<결합 프롬프트>"` 산출, stdin은 `Stdio::null()`로 의무 차단(안 하면 "Reading additional input from stdin..." 부작용, 실측); (M3) codex JSONL 파싱 — `parse_codex_agent_message`가 `item.completed`+`item.type=="agent_message"`에서 `item.text` 추출해 `ai://chunk`로 **정확히 1회** 통째로 emit(토큰 단위 분할 금지 — agent_message는 완성본 1회 도착, 실측), `turn.completed`에서 `ai://done`으로 마무리, stderr은 기존 `classify_stderr` 재사용; (M4) codex 감지 — `resolve_codex_binary`(PATH → 표준 위치) + `~/.codex/auth.json` 로그인 판정(claude의 `oauthAccount`과 대칭); (M5) provider별 모델 매핑 — claude{haiku,sonnet} / codex{gpt-5.5,gpt-5.5(v1 동일)}로 `AiModel::Haiku/Sonnet`을 그대로 재사용해 UI 변경 최소화; (M6) 무손상 가드 — 기존 claude 빌드/파싱/조립 경로는 동일 입력에 대해 동일 출력(바이트 동등 회귀 테스트로 검증). 4가지 사전 합의 결정(provider 자동 감지+수동 오버라이드 / 모델 매핑 테이블 / system+user "\n\n" 결합 / agent_message 1회 emit + 기존 플레이스홀더 재사용)은 "사전 합의 설계 결정(재검토 금지)" 섹션에 고정. |
| 0.0.3 | 2026-07-24 | jw | **구현 후 확인된 결함 2건 개정(REVISION)**. **결함 1 — codex JSONL 파서 계약 오류(치명, 재현 완료)**: 현행 REQ-AI9-009/010이 규정하고 구현(`stream.rs:147-186`)이 요구하는 라인 형태는 최상위 래퍼 `{"type":"event","event":{...}}`였으나, codex-cli 0.144.1의 **실측 출력은 래퍼가 없는 FLAT 형태**다(`build_codex_args` 산출 인자 벡터를 그대로 실행해 캡처). 결과 연쇄(소스 대조 검증): 두 파서가 항상 `None`/`false` → `ai://chunk` 0회·`ai://done` 0회 → EOF 폴백 `decide_outcome(None, stderr, false, true)` → codex는 성공 시에도 stderr에 `ERROR codex_models_manager::cache: failed to load models cache: missing field 'supports_reasoning_summaries'`를 남기므로 `classify_stderr` → `Other` → 사용자에게 "잠시 문제가 있었어요. 다시 시도해주세요."만 노출. 즉 **codex는 모든 요청에서 한 번도 동작하지 않았다**. 서브프로세스 호출부(`codex exec ... --json`, 인자 벡터, 격리 플래그, PATH 주입, 스크래치 cwd, stdin-null)는 정상이며 무변경. **근본 원인**: 본 SPEC 0.0.1의 Design Notes(현 §"`item.completed` JSON 형태 확정")가 "최상위 `type` vs `event.type`은 Run phase에서 실제 출력을 붙잡아 확정한다"고 명시했으나 **그 확정 단계가 수행되지 않았고**, `codex_cli.rs`의 `#[cfg(test)]` fixture 헬퍼 `agent_message_line`(codex_cli.rs:489)이 실제 출력 대신 래핑된 형태를 **날조**해 단위 테스트가 결함을 통과시켰다. 개정 내용: **REQ-AI9-009 개정**(FLAT `{"type":"item.completed","item":{"type":"agent_message","text":"..."}}`가 PRIMARY 계약, 래핑 형태는 문서화된 FALLBACK), **REQ-AI9-010 개정**(FLAT `{"type":"turn.completed",...}`가 PRIMARY, 래핑 FALLBACK), **AC-AI9-006 개정**(실측 캡처 fixture 필수 + 래핑 fallback fixture 병행, 손으로 날조한 형태를 유일 fixture로 쓰는 것을 금지), Design Notes에 0.144.1 실측 4줄 원문 + "성공 시에도 non-empty stderr" 사실 기록(파서 실패가 `parse`가 아닌 오도적 `other` 오류로 퇴화하는 이유). **결함 2 — provider 선택 UI가 대등하지 않음**: `SettingsModal.tsx`가 Claude 전용 상태 블록(180/196/209행의 리터럴 "Claude Code" + 배지·버전·온보딩 문구)을 하드코딩하고 codex는 `AiProviderSelect` 드롭다운(358행~) 속 옵션으로만 존재하며, `deriveConnectionState`가 섹션 전체를 단일 "유효 provider"로 환원해 claude=주도구/codex=부가설정처럼 보인다. 사용자 확정 목표 설계(라디오 행 목록, 드롭다운·세그먼트 토글 안은 명시적으로 기각)를 반영해 **REQ-AI9-026~032 신규 7건 추가**(행 렌더 계약 / 행별 상태 파생 / 미사용 행 선택 불가+사유 인라인 / registry 순서 구동 / 드롭다운 대체 및 영속화·IPC 무변경 / 정책 잠금 🔒 / 온보딩 진입점 보존), **AC-AI9-016~019 신규 4건 추가**. **REQ-AI9-020 개정**(0.0.1의 "v1 프론트 provider 선택 UI 전면 금지" → 본 개정으로 대등 행 UI가 범위에 편입되므로, 금지 대상을 "provider별 개별 설정 화면·채팅형 인디케이터·registry 외 하드코딩 목록"으로 축소), **AC-AI9-011 개정**(`src/components/` 변경 0건 어서션 → 변경 범위를 `SettingsModal.tsx` 1개 파일 + 그 테스트로 한정하는 어서션), Exclusions 해당 항목 개정, Delta 표에 `src/components/settings/SettingsModal.tsx`·`src/test/SettingsModal.test.tsx` [MODIFY] 행 추가. REQ 27→34, AC 15→19, 커버리지 대조표 동기화. |
| 0.0.2 | 2026-07-24 | jw | plan-auditor 1차 감사(FAIL 0.78/1.00) 피드백 반영 — 결함 6건 개정. **D1**(major): Unwanted REQ 8건(006/008/019/020/022/023/024/025)을 정규 EARS "IF [조건], then the system shall [응답]" 패턴으로 개정(SPEC-AI-001 REQ-AI-008/009/018/027 계보 정합). **D2**(major): 신규 **REQ-AI9-013a**(Event-Driven) 추가 — "chunk 1회 emit 후 turn.completed 누락 EOF" 시나리오를 담당, 기존 AC-AI9-007 case 3의 고아 AC를 이 REQ로 매핑. **D3**(minor): 신규 **REQ-AI9-007a**(Ubiquitous) 추가 — `CodexProvider::spawn`이 `combine_prompts → build_codex_args → spawn_codex` 순서로 호출하는 계약 명시(구현자가 combine_prompts 누락하는 회귀 방지). **D4**(minor): 신규 **AC-AI9-015**(수동 통합) 추가 — `~/.codex/AGENTS.md` 자동 로딩 차단 검증(`--ignore-user-config` + 빈 cwd가 사용자 홈 컨텍스트 오염을 방어하는지 확인, 32K 토큰 폭발 회귀). **D5**(minor): Delta 표에 `claude_cli.rs #[cfg(test)]` 행 추가(REQ-AI9-022 회귀 스냅샷; 프로덕션 코드 무변경, plan.md M1.1과 일치). **D6**(minor): plan.md M3.3에 `build_codex_command(binary, args, cwd) -> Command` 순수 함수 추출 힌트 추가(spawn_codex의 Command 구성을 단위 테스트 가능하게 분리, AC-AI9-005 자동화). REQ 25→27, AC 14→15, 커버리지 대조표 동기화. |
| 0.0.4 | 2026-07-24 | jw | **구현 후 확인된 결함 3건 개정(REVISION) — 결함 1·2의 잔여 동기화 완결 + 결함 3 신규**. **(A) 결함 1·2 잔여 동기화**: 0.0.3이 REQ 본문(009/010 개정, 026~032 신설, 020 범위 축소)과 Design Notes·Delta 표는 갱신했으나, **AC 표·AC-AI9-006/011 본문·커버리지 대조표·sibling `acceptance.md`/`plan.md`가 갱신되지 않은 채 남아** 0.0.3의 HISTORY 서술("AC-AI9-016~019 신규 4건 추가", "AC-AI9-011 개정", "커버리지 대조표 동기화")과 문서 실체가 어긋나 있었다. 본 개정에서 **AC-AI9-006 개정**(FLAT 실측 캡처가 PRIMARY fixture, 래핑은 FALLBACK fixture, 손으로 날조한 형태를 유일 fixture로 쓰는 것 금지 — 결함 1의 단위 테스트 통과 원인 차단), **AC-AI9-011 개정**(`src/components/` 변경 0건 어서션 → 변경 범위를 `SettingsModal.tsx` 1개 파일 + 그 테스트로 한정), **AC-AI9-016~019 실제 신설**(행 렌더 대칭성·registry 순서 구동 / 행별 상태 파생 순수 함수 / 미사용 행 선택 차단+인라인 사유+온보딩 진입점 보존 / 드롭다운 부재+영속화·IPC 무변경+정책 잠금 🔒), 커버리지 대조표를 REQ-AI9-026~032까지 확장했다. **(B) 결함 3 — AI 오류 카드가 파일 전환에도 살아남고 닫을 방법이 없음(스크린샷 확인)**: 원인 2개가 각각 소스에서 검증됨. **(3a)** 카드 레지스트리(`registerCardController`/`removeCardController`/`clearCardRegistry`, ai-suggestion-card.ts:759-789)의 유일한 일괄 정리 경로는 AI 토글 ON→OFF 전이(`aiOffEffects.ts:36`, `initAiToggleEffects`가 `AppLayout.tsx:84`에서 1회 등록) 뿐이고 **활성 문서 전환에 대응하는 정리 경로가 없다**. 파일 열기는 단일 진입점 `useFileSystem.openFile`(useFileSystem.ts:135)로 수렴하는데, 살아남은 카드의 `range`는 **이전 문서**의 위치를 가리키므로 새로 연 문서의 무관한 오프셋에 다시 앵커된다. **(3b)** `error` phase 렌더(ai-suggestion-card.ts:329-353)에 **닫기 컨트롤이 전혀 없다** — `other`는 [다시 시도]만, `network`는 버튼 없음, `login`은 [연결 안내 보기]만. [무시](`callbacks.onCancel`)는 `intruded` phase 전용이며, 나머지 종결 phase(`empty`/`cancelled-by-new`/`stale`)도 동일하게 버튼이 없다. 따라서 사용자는 수동으로도 카드를 치울 수 없다. 개정 내용: **REQ-AI9-033~038 신규 6건 추가**(활성 문서 전환 시 취소+고스트 정리+레지스트리 비움 / 독립 effects 모듈 + store subscribe + AppLayout 1회 등록 아키텍처 계약 / 문서 텍스트 무변경 HARD 불변 / `error` 3종 전 kind에 닫기 컨트롤 / 나머지 버튼 없는 종결 phase로 닫기 확장 / 닫기가 문서 텍스트를 바꾸거나 `streaming` phase에 렌더되는 것 금지), **AC-AI9-020~024 신규 5건 추가**(파일 전환 정리 3동작 / 무손상 불변 / `error` 닫기 3종 / 종결 phase 닫기 확장 / **회귀 AC — 파일 A에서 띄운 오류 카드가 파일 B를 연 뒤 사라짐**), Design Notes에 종결 phase 일괄 확장 근거 기록, Exclusions에 "`openFile`/`uiStore`/`fileStore` 내부 결합 금지"·"정리 시 문서 텍스트 수정 금지" 항목 추가, Delta 표에 `src/lib/ai/aiOffEffects.ts`(공유 추출 대상)·신규 `src/lib/ai/aiFileSwitchEffects.ts`·`ai-suggestion-card.ts`·`AppLayout.tsx` 및 대응 테스트 파일 [MODIFY]/[NEW] 행 추가. **불변 제약**: codex 인자 벡터·격리 플래그·모델 매핑·PATH 주입·스크래치 cwd·stdin-null 계약 무변경, claude 릴레이 경로 무변경(REQ-AI9-023 회귀 가드 유지). REQ 34→40, AC 15→24, 커버리지 대조표 동기화. |
| 0.0.5 | 2026-07-24 | jw | **구현 후 확인된 결함 4건째 개정(REVISION) — 결함 4 신규**. 결함 1~3은 구현 완료·품질 게이트 통과 상태이며 본 개정은 그 요구사항·AC를 **일절 변경하지 않는다**. **결함 4 — 빈 응답으로 완료되면 이어쓰기 고스트가 "작성 중…" 상태로 영구 고착(재현 완료)**: 임시 재현 테스트(`src/test/aiGhostEmptyDone.repro.test.ts`)가 `startGhostEffect` → `useAiStore.getState().completeRequest('', false)` 를 구동해 `placeholder = ✨ 작성 중…`, `buttons = ['✓ 넣기','✕ 지우기']` 를 **동시에** 관측했다(사용자 스크린샷과 일치). **3개 코드 지점이 맞물린 기전**: (1) `src/store/aiStore.ts:65-72` `reduceCompleteRequest` 가 `streamBuffer` 를 최종 `result` 로 **덮어쓴다** — `result === ''` 이면 버퍼가 비워진다. (2) `ai-ghost-text.ts:848-849` `ghostStoreBridge` 가 그 버퍼를 그대로 `setGhostTextEffect.of('')` + `setGhostStatusEffect.of('done')` 으로 미러링한다. (3) `ai-ghost-text.ts:257-269` `ghostDecorations` 는 `text === ''` 를 "첫 청크 미도착"으로 해석해 `GhostPlaceholderWidget`("✨ 작성 중…")을 렌더하고, 동시에 `status === 'done'` 이므로 `GhostControlsWidget` 이 [✓ 넣기]/[✕ 지우기]를 렌더한다. 이후 어떤 이벤트도 오지 않아 상태가 **영구 고착**되며, `confirmGhostCommand`(`ai-ghost-text.ts:468-470`)가 `if (!ghost || !ghost.text) return false;` 로 가드하므로 **[✓ 넣기]는 조용한 no-op** 이다 — 사용자에게는 "계속 작성 중인데 넣기는 아무 반응이 없음"으로 보이고 탈출 경로는 [✕ 지우기]/Esc 뿐이다. **빈 `result` 를 만드는 실제 프로덕션 경로 2개**: (a) **codex** — `src-tauri/src/ai/codex_cli.rs` `relay_codex_process` 의 `turn.completed` 분기가 `let result = last_message.clone().unwrap_or_default();` 이므로 `agent_message` 없이 완료된 턴은 `result == ""` 인 `ai://done` 을 emit한다(이 경로는 결함 1 파서 수정이 머지된 **이후에야 도달 가능**해졌다 — 그 전에는 두 파서가 모두 실패해 오류로 퇴화했다). (b) **claude** — `parse_final_result` 가 빈 문자열을 반환하는 경우. **누락이지 의도가 아니라는 결정적 근거**: 카드 경로는 동일 상황을 이미 올바르게 처리한다 — `ai-suggestion-card.ts:81-88` `reduceCard` 가 빈/동일 최종 텍스트를 전용 `empty` phase("이미 자연스러워서 바꿀 곳이 없어요", v0.0.4부터 [닫기] 병기)로 라우팅한다. **고스트 경로에만 `empty` 대응물이 없다**. **사용자 확정 목표 동작(재검토 금지)**: 고스트 경로 요청(`feature === 'section-fill'` — `ai-ghost-text.ts:836-840` `@MX:NOTE` 기준 섹션 채우기·문서 끝 이어쓰기·자유 위치 이어쓰기 전부)이 실질적 빈 결과로 종결되면 `ℹ 더 쓸 내용을 찾지 못했어요` 안내 + [✕ 닫기] 1개를 렌더한다. "✨ 작성 중…" 과 [✓ 넣기]는 이 상태에서 **금지**. **기각된 대안(기록용)**: (i) 무통보 자동 제거 — 무통보 취소 금지 원칙(REQ-AI-006/P7) 위배, (ii) 안내에 [다시 요청] 추가 — 고스트 경로는 이미 별도 ↻ 재요청(`reRequestGhost`, `ai-ghost-text.ts:439`)을 제공하며, `ai-ghost-text.ts:347` 에 문서화된 클래스 분리 제약(기존 테스트가 done 상태 `.cm-ai-ghost-btn` 개수를 정확히 2개로 단언)과도 충돌한다. **백엔드 계약 결정**: Rust 측은 **무변경** — 빈 `result` 의 `ai://done` emit은 두 provider 모두에서 여전히 적법하며, 빈 결과의 표현은 **프론트 단일 지점**이 담당한다(codex/claude 동작 경로 단일화). 개정 내용: **REQ-AI9-039~044 신규 6건 추가**(고스트 terminal-empty 렌더 계약 / "실질적 빈 값" 정의 / 대기 플레이스홀더·[넣기] 금지 / 문서 무변경·무통보 제거 금지 / 백엔드 무변경 계약 / [다시 요청] 금지 + 컨트롤 개수 불변), **AC-AI9-025~027 신규 3건 추가**(terminal-empty 렌더·정의 / **재현 파생 회귀 AC** / 무손상·무변경·기존 고스트 테스트 회귀 없음), plan.md **M9** 신설, Delta 표에 `ai-ghost-text.ts` [MODIFY] + 테스트 파일 행 추가(`src/store/aiStore.ts` 는 **의도적으로 미변경**), Design Notes·Exclusions 갱신. REQ 40→46, AC 24→27, 커버리지 대조표 동기화. |
| 0.0.6 | 2026-07-24 | jw | **구현 후 확인된 결함 5건째 개정(REVISION) — 결함 5 신규**. 결함 1~4는 구현 완료·품질 게이트 통과 상태이며 본 개정은 그 요구사항·AC를 **일절 변경하지 않는다**. **결함 5 — "고급 모델 사용" 토글이 codex 에서 완전히 무효(provider parity gap, 소스 대조로 확인)**: 프론트는 토글을 문자열 `'sonnet' \| 'haiku'` 로 환원해(`SettingsModal.tsx:46-48` `resolveModel`, `ai-selection-toolbar.ts:168-170` `resolveModel`) IPC `model` 필드로 보내고, `AiModel::from_opt`(`provider.rs:28-34`)가 이를 `Sonnet \| Haiku` 로 매핑한다. 그런데 `codex_model_arg`(`codex_cli.rs:41-45`)는 **`Haiku` 와 `Sonnet` 을 모두 `"gpt-5.5"` 로** 매핑하고, `build_codex_args`(`codex_cli.rs:60-80`)는 `-c model_reasoning_effort="medium"` 을 **하드코딩**한다. 결과적으로 codex 가 선택된 상태에서 고급 모델 토글을 켜고 끄면 스폰되는 인자 벡터가 **단 1바이트도 달라지지 않는다** — 죽은 토글이다. 라벨도 codex 사용자에게 문자 그대로 틀렸다: `고급 모델 사용 (sonnet — 더 정확, 더 느림)` 과 `aria-label="고급 모델 사용 (sonnet)"`(`SettingsModal.tsx:286,291`)이 codex 선택 중에도 Anthropic 모델명을 명시한다. 이는 SPEC-AI-009 자신의 목표("codex 를 대등한 두 번째 프로바이더로")의 parity gap 이므로 별도 SPEC 이 아니라 본 SPEC 에 귀속한다. **claude 쪽 비대칭(비자명 지점)**: claude 에서 codex 의 reasoning effort 에 대응하는 것은 모델명이 아니라 **사고 예산**이다 — `spawn_claude`(`claude_cli.rs:179`)가 `.env("MAX_THINKING_TOKENS", "0")` 을 **두 티어 모두에** 무조건 설정해 claude 의 추론을 전면 비활성화하고 있다. **사용자 확정 목표 동작(재검토 금지)**: 토글은 provider 별로 모델과 추론 강도를 **함께** 바꾼다 — claude 기본 = `--model haiku` + `MAX_THINKING_TOKENS=0`(현행 유지), claude 고급 = `--model sonnet` + **env 를 아예 설정하지 않음**(claude CLI 자체 기본값에 위임); codex 기본 = `--model gpt-5.5` + `model_reasoning_effort="medium"`(현행 유지), codex 고급 = `--model gpt-5.5` + `model_reasoning_effort="high"`. **사고 예산에 숫자를 발명하지 않는 근거**: 하드코딩 상수(예: 4000)는 누구도 사후에 정당화할 수 없는 무출처 매직 넘버가 되지만 "미설정 = CLI 기본값"은 자기문서적이며 CLI 버전업에도 안정적이다. **라벨 수정**: 라벨·`aria-label` 의 `"sonnet"` 하드코딩을 제거하고 **현재 선택된 provider**(결함 2에서 도입한 `aiSelectedProvider` 키)에서 표시를 파생한다 — 3번째 provider 가 등록되면 깨지는 provider 별 리터럴 테이블을 새로 만들지 않는다(기존 `providerDisplayName` 의 `?? id` 폴백 재사용). **명시적 범위 밖(사용자 확정)**: 컨텍스트 문자 상한 `INLINE_SIDE_MAX = 1000` / `SECTION_TAIL_MAX = 1500` / `CONTINUE_HEAD_MAX = 1500`(`prompt.rs:10-14`)은 **변경하지 않는다** — 이 값들은 모델 컨텍스트 한계가 아니라 지연/초점 설계값이며(세 모델 모두 컨텍스트 창이 1500자를 압도한다) 고급 티어에서 올릴 측정 근거가 없고 지연만 늘린다. 재검토는 `truncated` 플래그가 실제로 얼마나 자주 뜨는지 측정에서 시작해야 하며, 본 개정은 이를 **연기 항목**으로 못박아 향후 구현자가 "친절하게" 상수를 올리는 것을 막는다. **`AiModel` 명명 결정(재론 금지)**: `Haiku`/`Sonnet` 은 이제 provider 무관 티어(기본/고급)의 의사 이름이지만 **(a) enum 이름을 유지하고 관례로 티어 취급 + 매핑표 문서화**를 택한다 — 근거는 Design Notes "AiModel 명명 결정" 참조(REQ-AI9-019 계약 유지). 개정 내용: **REQ-AI9-045~050 신규 6건 추가**(codex 티어별 reasoning effort 파생 / claude 티어별 모델+사고 env 파생 / 숫자 사고 예산 하드코딩 금지 / provider 인지 라벨·aria-label / 프론트 모델명 리터럴·무폴백 테이블 금지 / 양 provider 기본 티어 무변경 회귀 가드), **AC-AI9-028~030 신규 3건 추가**(codex 인자 벡터 스냅샷 — 기본 vs 고급이 `model_reasoning_effort` 값 **한 원소만** 다름 / claude 기본 티어 `MAX_THINKING_TOKENS=0` 유지·고급 티어 미설정 / 라벨이 선택 provider 를 따름), plan.md **M10** 신설, Model Mapping 표에 reasoning 열 추가, Delta 표·Design Notes·Exclusions 갱신. REQ 46→52, AC 27→30, 커버리지 대조표 동기화. |
| 0.0.7 | 2026-07-25 | jw | **v0.0.6의 라벨 설계 결정만 교체(AMENDMENT) — 나머지 전부 유지**. v0.0.6은 라벨에 실제 모델명을 넣지 않고 `고급 모델 사용 ({표시명} 고급 티어 — …)` 형태의 **provider 표시명만**으로 구성하기로 하고, "백엔드가 매핑 소유자로서 표시 문자열을 내려주는 안"은 IPC 표면 확대 비용을 이유로 **연기 항목**으로 두었다(v0.0.6 Design Notes "라벨을 provider 표시명만으로 구성하는 이유"). 사용자가 그 연기 항목을 **채택**하기로 확정했으므로 본 개정이 이를 반영한다 — v0.0.6 HISTORY 행은 **교체된 결정의 기록으로 그대로 보존**한다. **채택 설계**: `ProviderStatus`(`src-tauri/src/ai/provider.rs`, `ai_detect_providers` 로 노출, `src/lib/tauri/ipc.ts` 의 `AiProviderStatus` 로 타입화)에 **`advanced_model_label: Option<String>`**(IPC camelCase `advancedModelLabel?: string`) 필드를 추가해 각 provider 의 **고급 티어 표시 문자열**을 백엔드가 내려준다 — claude → `sonnet`, codex → `gpt-5.5 · 높은 추론`. **[HARD] 단일 소스**: 이 값은 실제 인자를 만드는 **동일한 중앙 매핑 함수**에서 파생되어야 한다 — codex 는 `codex_model_arg(AiModel::Sonnet)` + `codex_reasoning_effort(AiModel::Sonnet)`, claude 는 `AiModel::as_arg(AiModel::Sonnet)`. 이것이 이 설계의 **전부**다: 라벨이 실제로 스폰되는 것과 **구조적으로 어긋날 수 없게** 만드는 것. 따라서 같은 문자열을 다른 어디에도 복제하는 것을 금지한다 — 두 번째 Rust 상수도, 어떤 프론트 리터럴도 금지다. **프론트**: 백엔드가 보낸 문자열을 **그대로** 렌더하고(목표 라벨 `고급 모델 사용 (gpt-5.5 · 높은 추론 — 더 정확, 더 느림)` / `고급 모델 사용 (sonnet — 더 정확, 더 느림)`, `aria-label` 도 동일 소스), 필드가 **부재·빈 문자열·공백만**인 경우(3번째 provider 나 구버전 백엔드)에만 v0.0.6의 `providerDisplayName` 기반 형태로 폴백한다. **v0.0.6 대비 정정 사항(명시)**: v0.0.6 AC-AI9-030은 `src-tauri/src/ai/provider.rs` 와 `src/lib/tauri/ipc.ts` 를 **"diff 0줄"** 무변경 파일로 단언했다 — 본 개정이 두 파일에 필드를 추가하므로 **그 제약은 조용히 위반되는 것이 아니라 여기서 명시적으로 정정된다**. `provider.rs` 는 이제 "`AiModel` enum·`as_arg`·`from_opt` 무변경, `ProviderStatus` 는 필드 1개 추가 허용"로 좁혀지고, `ipc.ts` 는 "`AiRequestArgs` 무변경, `AiProviderStatus` 는 선택 필드 1개 추가 허용"로 좁혀진다. `prompt.rs`·`uiStore.ts`·`ai-selection-toolbar.ts` 의 diff 0줄 제약은 **그대로 유지**된다. 개정 내용: **REQ-AI9-048 개정**(라벨을 백엔드 필드에서 렌더, 폴백은 REQ-AI9-053), **REQ-AI9-049 개정**(금지 대상을 "프론트 모델명 리터럴 + **두 번째 Rust 상수** + 폴백 없는 테이블"로 확장), **REQ-AI9-050 (d) 개정**(무변경 목록에서 `provider.rs`·`ipc.ts` 를 범위 축소된 형태로 교체), **REQ-AI9-051~053 신규 3건 추가**(필드 정의·정확한 shape / 중앙 매핑 단일 소스 파생 HARD + 복제 금지 / 프론트 렌더 + 폴백 3단), **AC-AI9-030 개정**(백엔드가 보낸 문자열이 라벨에 포함됨을 단언 + 폴백 경로 단언), **AC-AI9-031 신규 1건 추가**(백엔드 필드 값이 인자 조립과 **동일 함수**에서 파생됨을 Rust 단위 테스트로 단언 — 향후 매핑 변경이 라벨을 거짓말시키지 못하게), plan.md **M10.3 전면 개정**(Rust 필드 → IPC 타입 → 프론트 렌더 → 폴백 4단계), Delta 표에 `provider.rs`·`detect.rs`·`ipc.ts` 행 추가, Design Notes의 "연기 항목" 서술을 **채택된 설계**로 갱신(프론트 복제가 결함 5의 형태 그 자체라는 근거는 유지). **v0.0.6에서 그대로 유지되는 것**: `AiModel` 명명 결정 (a)(이름 유지 + 관례상 티어), 컨텍스트 문자 상한 3종 범위 밖 결정, REQ-AI9-045/046/047 및 AC-AI9-028/029 전부. REQ 52→55, AC 30→31, 커버리지 대조표 동기화. |

## Summary

`mdedit`(Tauri v2 + React 18 + TypeScript + CodeMirror 6)의 AI 글쓰기 도우미는 현재 `claude` CLI를 단일 서브프로세스 프로바이더로 호출한다. 본 SPEC은 **`codex` CLI(codex-cli 0.144.1)**를 두 번째 프로바이더로 `ProviderRegistry`에 추가해, 사용자 기기에 두 CLI 중 하나라도 설치·로그인되어 있으면 AI 기능이 동작하도록 만든다. `provider.rs`의 trait 설계는 이미 이 확장을 전제로 했다(provider.rs:5-6, 57-74).

핵심 특성:

- **격리 우선**: codex는 `--ignore-user-config --skip-git-repo-check --ephemeral --sandbox read-only`와 빈 스크래치 cwd를 결합해 부작용을 차단한다(`CODEX_HOME` 격리는 시도하지 않는다 — 인증이 풀림, 실측).
- **다른 스트림 포맷**: codex는 `--json`(JSONL)을 내보내며, `claude`의 `stream-json`(`text_delta` 토큰 스트리밍)과는 완전히 다른 이벤트 모델을 쓴다. `item.completed`의 `agent_message`가 **완성본을 한 번에** 실어 보낸다(토큰 단위 스트리밍 아님).
- **UI 계약 재사용 + 제한된 표면 변경** *(v0.0.4 개정, v0.0.6 보강)*: 프론트의 기존 `aiAdvancedModel` 토글(uiStore, **상태 키·`resolveModel` 반환 도메인은 v0.0.6에서도 무변경** — 결함 5는 라벨 문구와 백엔드 인자 파생만 손댄다)·`ai://chunk|done|error` 이벤트 계약·`waitNotice.ts`의 "✨ 작성 중…" 플레이스홀더·`GhostPlaceholderWidget`은 그대로 재사용된다. 다만 구현 후 확인된 결함 2·3을 수정하기 위해 **설정 다이얼로그의 대등 provider 행 목록**(모듈 7)과 **AI 카드 생명주기 정리·닫기**(모듈 8)가 범위에 편입되었다. 영속화 키·IPC 계약은 여전히 무변경이다. ~~"provider 전환 UI는 v1에서 도입하지 않는다"(v0.0.1)~~는 폐기됨.
- **하위호환 절대 보존**: 기존 `claude` 경로(`build_claude_args`, `parse_text_delta`, `ClaudeProvider`, `spawn_claude`, prompt.rs 조립)는 바이트 단위로 동등하게 유지된다(회귀 스냅샷 테스트로 검증).

## 사전 합의 설계 결정 (재검토 금지)

> 아래 4가지는 사용자가 기능 합의 단계에서 확정한 결정이다. Run phase에서 재검토하지 않는다. 요구사항 변경이 필요하면 SPEC 개정(버전 올림)으로만 반영한다.

1. **provider 선택 — 자동 감지 + 수동 오버라이드**
   - 두 CLI 중 하나라도 설치+로그인 → AI 기능 동작.
   - 자동 감지 우선순위: **claude 먼저, 그다음 codex**. `claude`가 설치된 환경에서는 `claude`가 기본 프로바이더다.
   - 수동 오버라이드: 프론트 `AiRequestArgs.providerId`(이미 존재, `provider_id: Option<String>`, mod.rs:89)로 특정 provider 강제 선택.
   - `detect.rs`의 기존 `ProviderStatus { id, installed, version?, loggedIn }` 구조체를 그대로 재사용(provider.rs:47-55). 신규 필드·새 enum 금지.

2. **모델 매핑 — provider별 테이블 + 기존 토글 재사용**
   - `AiModel::Haiku`/`AiModel::Sonnet`(provider.rs:13-17)을 **"기본/고급" 의사 티어로 재해석**해 그대로 유지. enum 확장·이름 변경 금지.
   - provider별 매핑:
     | Provider | `AiModel::Haiku`(기본) | `AiModel::Sonnet`(고급) |
     |----------|------------------------|--------------------------|
     | claude   | `haiku`                | `sonnet`                 |
     | codex    | `gpt-5.5`              | `gpt-5.5` (v1 일단 동일) |
   - 프론트의 기존 `aiAdvancedModel` 토글(`uiStore`)을 그대로 재사용 — UI 변경·새 토글 금지.

3. **system_prompt 전달 — `"\n\n"` 결합해 positional 1개로**
   - `prompt.rs`가 산출하는 `(system_prompt, user_prompt)` 쌍을 `format!("{}\n\n{}", system, user)`로 단순 결합해 codex의 **positional 인자 정확히 1개**로 넘긴다(codex는 `claude --system-prompt` 분리 불가, 실측).
   - 결합 문자열은 codex `exec`의 마지막 인자(따옴표로 감싼 단일 arg).

4. **스트리밍 UX — agent_message 도착 시 `ai://chunk` 1회 + 기존 플레이스홀더 재사용**
   - `item.completed`/`agent_message` 도착 순간, 해당 본문 전체를 `ai://chunk` payload로 **정확히 1회** emit(토큰 단위 분할 금지 — codex 자체가 완성본을 한 번에 보냄, 실측).
   - 본문 도착 전까지 기존 "✨ 작성 중…" 플레이스홀더(`waitNotice.ts`, `GhostPlaceholderWidget`)를 재사용. 새 "대기 중" UI 금지.
   - `turn.completed`에서 `ai://done`(payload: `result`=마지막 agent_message 본문, `truncated`=기존 정책 전달)으로 마무리.

## Background & Rationale

현재 `mdedit`의 AI 기능(인라인 편집·고스트 텍스트·섹션 채우기·다이어그램 생성 등)은 전부 `claude` CLI에 단일 의존한다. 사용자 기기 분포를 보면 `claude` 대신 `codex`가 설치된 경우가 있어, 단일 프로바이더 고정이 진입 장벽으로 작용한다. `provider.rs`는 본 확장을 대비해 `AiProvider` trait(`id`/`detect`/`spawn`/`capabilities`)과 `ProviderRegistry`(다중 provider·`route(provider_id)`)를 미리 확정했다(provider.rs:5-6, 65-127).

`codex` CLI는 OpenAI 계열 사용자에게 익숙하고, `claude`와는 별개 인증(`~/.codex/auth.json`)·별도 출력 포맷(JSONL)을 쓴다. 본 SPEC은 이 차이를 trait 뒤에 숨기고, 호출부(mod.rs:122 `ai_request`)는 동일한 `route → spawn → relay` 경로를 유지하게 만든다.

기술 컨텍스트(소스 근거, 실측):

- **provider trait 확장 지점**: `provider.rs:65-74`의 `AiProvider` trait 4개 메서드 + `Capabilities { supports_streaming, typical_latency_ms }`(provider.rs:58-62). codex는 `supports_streaming = false`에 가까운 의미(`agent_message` 1회 도착 = UI 입장에서 스트리밍이 아닌 한 번에 표시)로 설정해 `relay` 계약을 분기 가능하게 한다(다만 `ai://chunk` 1회 emit으로 프론트 호환성은 유지).
- **레지스트리 등록 지점**: `claude_cli.rs:305-307`의 `claude_registry()`가 현재 `vec![Box::new(ClaudeProvider::new())]`로 단일 provider를 등록한다. 본 SPEC은 동일 지점에 codex를 추가 등록하거나 새 `default_registry()`를 신설한다(REQ-AI9-003).
- **route 계약 재사용**: `ProviderRegistry::route(provider_id)`(provider.rs:121-126)는 `Some(id)`→이름 조회, `None`→`default_provider()`(첫 등록). `mod.rs:173-175`는 이미 `registry.route(args.provider_id.as_deref())`를 호출하고 있어 **IPC부터 route까지의 배선은 이미 완성되어 있다** — 본 SPEC이 다루는 것은 registry의 내용과 각 provider의 구현뿐이다.
- **빈 스크래치 cwd 재사용**: `claude_cli.rs:106-111`의 `ensure_scratch_dir(app_data_dir)`이 앱 데이터 디렉토리 하위 `ai-scratch/`를 빈 디렉토리로 보장한다. codex도 동일 cwd를 쓴다(`-C <scratch>`).
- **stdin null 패턴**: `claude_cli.rs:180`는 `.stdin(Stdio::null())`로 claude의 stdin 대기를 차단한다. codex도 동일 패턴 적용 — 안 하면 codex가 "Reading additional input from stdin..."을 출력하며 stdin을 읽으려 시도한다(실측, 4회 테스트).
- **절대경로 스폰**: `claude_cli.rs:172-174`는 `detect::claude_binary()`가 해석한 절대경로로 스폰한다(GUI 최소 PATH 우회). codex도 `detect::codex_binary()` 절대경로를 쓴다.
- **스트림 파서 분리 필요**: `stream.rs:40-55`의 `parse_text_delta`는 claude 전용(`stream_event`/`content_block_delta`/`text_delta` 경로). codex JSONL은 `item.completed`/`agent_message`/`turn.completed` 등 다른 이벤트 타입을 써서 **별도 파서가 필요**하다(stream.rs에 신규 함수 추가).
- **stderr 분류 재사용**: `stream.rs:78-126`의 `classify_stderr`는 login/network/other 휴리스틱을 표준화해 둔다. codex stderr에도 그대로 적용한다 — codex 전용 분류 로직 중복 금지.
- **로그인 판정 대칭**: `detect.rs:99-107`의 `is_logged_in(home)`은 `~/.claude.json`의 `oauthAccount` 키 또는 레거시 자격 파일로 claude 로그인을 판정한다. codex는 `~/.codex/auth.json` 존재로 판정한다(단일 휴리스틱).
- **AI 토글 무관**: `mod.rs:128-130`의 `ai_policy_disabled` kill-switch는 provider 무관. SPEC-AI-005의 `effectiveAiEnabled` 토글도 provider 무관. 본 SPEC은 토글 분기를 추가하지 않는다.

## Environment & Assumptions

- **codex CLI**: `codex-cli 0.144.1`(실측, 2026-07-24). 향후 상위 버전은 `-c 'model_reasoning_effort="medium"'` 등 플래그 호환성을 전제로 한다(플래그 시퀀스는 REQ-AI9-004에 고정, codex-cli 버전업 시 단위 테스트로 회귀 포착).
- **작동 명령(macOS, 빈 스크래치 cwd)**:
  ```
  codex exec -C <빈TMP> --ignore-user-config --skip-git-repo-check --ephemeral \
    --sandbox read-only --model gpt-5.5 -c 'model_reasoning_effort="medium"' \
    --json "<결합된 프롬프트>"
  ```
- **백엔드**: Rust `src-tauri/src/ai/{mod.rs, provider.rs, claude_cli.rs, stream.rs, detect.rs, prompt.rs}`. `Cargo.toml` 의존성 무변경(신규 런타임 크레이트 금지, `std::process::Command` 사용).
- **프론트엔드** *(v0.0.4 개정)*: React 18, TypeScript strict. `src/lib/tauri/ipc.ts`의 `AiRequestArgs` 타입에 `providerId?`가 이미 존재(mod.rs:89와 대칭). ~~"본 SPEC은 프론트 파일을 수정하지 않는다"(v0.0.1)~~는 폐기 — 결함 2·3 수정으로 `src/components/settings/SettingsModal.tsx`, `src/components/layout/AppLayout.tsx`, `src/components/editor/extensions/ai-suggestion-card.ts`, `src/lib/ai/aiOffEffects.ts` + 신규 `src/lib/ai/aiFileSwitchEffects.ts`가 변경 범위에 포함된다(Delta 표 참조). *(v0.0.5)* 결함 4 수정으로 `src/components/editor/extensions/ai-ghost-text.ts` 가 추가된다. *(v0.0.6)* 결함 5 수정으로 `SettingsModal.tsx` 의 `AdvancedModelToggle` 라벨 파생이 다시 변경 범위에 들어오고, 백엔드는 `codex_cli.rs`(reasoning effort 파생)·`claude_cli.rs`(`spawn_claude` 사고 env 파생)가 포함된다. *(v0.0.7)* 백엔드 공급 라벨 채택으로 `provider.rs`(`ProviderStatus` 선택 필드 1개)·`detect.rs`(생성 지점 `None`)·`src/lib/tauri/ipc.ts`(`AiProviderStatus` 선택 필드 1개)가 추가된다. 영속화 키(`aiSelectedProvider`·`aiAdvancedModel`)와 `AiRequestArgs` IPC 타입(`model?: 'haiku'|'sonnet'`, `providerId?`), `src/store/aiStore.ts`, `prompt.rs`, `AiModel` enum, Rust 릴레이 계약은 무변경.
- **개발 모드**: `quality.yaml` `development_mode: tdd`. 모듈 7·8·9의 프론트 변경은 Vitest + Testing Library로 **실패하는 테스트를 먼저** 작성한 뒤 구현한다(RED-GREEN-REFACTOR). AC-AI9-016~027은 이 순서로 바로 옮길 수 있도록 관측 가능한 단언으로 기술되어 있다.
- **IPC 계약**: `AiRequestArgs`의 camelCase → snake_case 매핑(mod.rs:84-116). 본 SPEC은 새 필드를 추가하지 않는다(`provider_id`만 재사용).
- **출력 이벤트**: `ai://chunk`(`{requestId, text}`), `ai://done`(`{requestId, result, truncated?}`), `ai://error`(`{requestId, kind, message, cancelledBy?}`) — 전부 기존 계약(claude_cli.rs:27-54).
- **테스트 환경**: Rust `#[cfg(test)]` 단위 테스트(claude_cli.rs:309-, detect.rs:290-, stream.rs:128- 패턴). codex 감지 함수는 순수/부작용 분리해 단위 테스트. codex JSONL 파서는 JSON fixture 기반 단위 테스트.

## Requirements (EARS)

> 변경 유형 태그: **[NEW]** = 신규 파일/함수/메서드, **[MODIFY]** = 기존 파일 수정, **[EXISTING]** = 기존 자산 변경 없이 재사용(회귀 가드 대상). REQ ID 접두사 `AI9`는 SPEC-AI-001의 `REQ-AI-XXX`와 충돌하지 않는다(SPEC-AI-003의 `AI3`, SPEC-AI-005의 `AI5`, SPEC-AI-006의 `AI6` 계보).

### 모듈 1 — Provider 추상화 확장 (registry에 codex 등록, 자동 감지, 수동 오버라이드)

- **REQ-AI9-001** (Ubiquitous) **[MODIFY]**: The system **shall** 항상 `ProviderRegistry`에 `claude`와 `codex` 프로바이더를 **정확히 2개** 등록하고, `claude`를 첫 번째(등록 순서상 default)로 둔다. `registry.ids()`는 `["claude", "codex"]`를 반환한다.
- **REQ-AI9-002** (Ubiquitous) **[MODIFY]**: The system **shall** `default_provider()`가 "installed && logged_in" 조건을 만족하는 첫 프로바이더를 자동 선택하게 한다. `claude`가 설치+로그인된 환경에서는 `claude`가 기본이고, `claude`가 미설치 또는 미로그인이고 `codex`만 사용 가능하면 `codex`가 기본이다. 둘 다 사용 불가면 `None`(기존 "사용 가능한 AI 도구가 없어요" 오류로 연결).
- **REQ-AI9-003** (Ubiquitous) **[EXISTING]**: The system **shall** `ProviderRegistry::route(args.provider_id.as_deref())`(provider.rs:121-126) 호출을 그대로 재사용해, `providerId`가 명시되면 해당 provider, 생략되면 REQ-AI9-002의 자동 감지 결과로 라우팅한다. `mod.rs:173-175`의 호출부 변경 없음(이미 `route(provider_id)`를 쓰고 있음).

### 모듈 2 — codex 인자 조립 (검증된 플래그, stdin null, system+user 결합)

- **REQ-AI9-004** (Ubiquitous) **[NEW]**: The system **shall** `build_codex_args(model: AiModel, combined_prompt: &str) -> Vec<String>` 순수 함수를 `codex_cli.rs`에 제공한다. 산출 인자는 정확히 다음 순서여야 한다:
  ```
  ["exec", "-C", "<scratch>", "--ignore-user-config", "--skip-git-repo-check",
   "--ephemeral", "--sandbox", "read-only", "--model", "<model_arg>",
   "-c", "model_reasoning_effort=\"medium\"", "--json", "<combined_prompt>"]
  ```
  여기서 `<scratch>`는 빈 스크래치 디렉토리 절대경로, `<model_arg>`는 REQ-AI9-019의 매핑 결과, `<combined_prompt>`는 REQ-AI9-006의 결합 결과다. 인자 순서·개수는 codex-cli 0.144.1 실측을 기준으로 한다.
- **REQ-AI9-005** (Ubiquitous) **[NEW]**: The system **shall** codex 호출 시 `system_prompt`와 `user_prompt`를 `format!("{}\n\n{}", system_prompt, user_prompt)`로 결합해 단일 positional 인자로 전달한다. 결합 문자열은 codex `exec`의 마지막 인자(REQ-AI9-004)이다. `--system-prompt` 같은 분리 플래그를 쓰지 않는다(codex 미지원, 실측).
- **REQ-AI9-006** (Unwanted) **[NEW]**: **IF** `spawn_codex` 호출 시 codex 프로세스의 stdin이 열려 있으면(Stdio::null()로 차단되지 않은 상태), **then** the system **shall** 반드시 `.stdin(Stdio::null())`로 stdin을 차단한다 — 그렇지 않으면 codex가 "Reading additional input from stdin..."을 출력하며 환경 의존적 부작용을 유발한다(실측). `claude_cli.rs:180`과 동일 패턴.
- **REQ-AI9-007** (Ubiquitous) **[NEW]**: The system **shall** `spawn_codex(args: &[String], cwd: &Path) -> Result<Child, String>`를 제공한다. 이 함수는 (a) `detect::codex_binary()`가 해석한 절대경로로 스폰(GUI 최소 PATH 우회), (b) `process_util::no_window`로 Windows 콘솔 깜빡임 차단, (c) `cwd`를 빈 스크래치 디렉토리로 고정, (d) `.stdin(Stdio::null())`, `.stdout(Stdio::piped())`, `.stderr(Stdio::piped())`를 적용한다(`spawn_claude` claude_cli.rs:170-185과 동일 구조).
- **REQ-AI9-007a** (Ubiquitous) **[NEW]**: The system **shall** `CodexProvider::spawn(request, cwd)`이 **정확히 다음 순서**로 내부 호출을 수행하게 한다: (1) `combine_prompts(request.system_prompt, request.user_prompt)` → 결합 문자열, (2) `build_codex_args(request.model, combined)` → 인자 벡터, (3) `spawn_codex(args, cwd)` → 자식 프로세스. 이 순서를 건너뛰거나 임의로 바꾸는 구현을 허용하지 않는다(구현자가 `combine_prompts` 누락 시 codex가 빈 프롬프트를 받거나 system/user가 분리되어 전달되는 회귀 방지).
- **REQ-AI9-008** (Unwanted) **[NEW]**: **IF** 코드가 `CODEX_HOME` 환경변수를 설정해 codex 인증·설정을 격리하려 시도하면, **then** the system **shall** 이를 거부한다 — codex 도움말에 명시된 대로 auth도 `CODEX_HOME`을 써서 이 값을 바꾸면 인증이 풀린다(실측). 격리는 빈 cwd(`-C <scratch>`) + `--ignore-user-config` + `--sandbox read-only` 조합으로만 달성한다(`build_codex_args`가 반드시 이 3개 플래그를 포함하는지 단위 테스트로 검증).

### 모듈 3 — codex JSONL 파싱 (agent_message → ai://chunk 1회, turn.completed → done)

- **REQ-AI9-009** (Ubiquitous) **[MODIFY]** *(v0.0.3 개정 — 결함 1)*: The system **shall** `parse_codex_agent_message(line: &str) -> Option<String>` 순수 함수를 `stream.rs`에 제공한다. 이 함수가 인정하는 라인 형태는 다음 두 가지이며, 우선순위가 명시된다:
  - **PRIMARY(실측, codex-cli 0.144.1)** — 래퍼 없는 FLAT 형태: 최상위 `type == "item.completed"` 이고 `item.type == "agent_message"` 이면 `item.text` 문자열을 `Some(text)`로 반환한다.
    ```
    {"type":"item.completed","item":{"id":"item_0","type":"agent_message","text":"Hi"}}
    ```
  - **FALLBACK(레거시/미래 호환)** — 래핑 형태: 최상위 `type == "event"` 이고 `event.type == "item.completed"` 이며 `event.item.type == "agent_message"` 이면 `event.item.text`를 `Some(text)`로 반환한다.
    ```
    {"type":"event","event":{"type":"item.completed","item":{"type":"agent_message","text":"Hi"}}}
    ```
  두 형태 **모두** 인정해야 한다(사용자 확정 결정) — 향후 codex CLI가 래퍼를 재도입해도 회귀하지 않게 하기 위함이다. 그 외 이벤트 타입(`thread.started`, `turn.started`, `turn.completed`, `item.type != "agent_message"`)이거나 JSON 파싱 실패 시 `None`을 반환한다(panic 없음, raw JSON 노출 없음 — `parse_text_delta` stream.rs:40-55와 동일한 안전 계약).
- **REQ-AI9-010** (Ubiquitous) **[MODIFY]** *(v0.0.3 개정 — 결함 1)*: The system **shall** `parse_codex_turn_completed(line: &str) -> bool` 순수 함수를 제공한다. 인정 형태는 REQ-AI9-009와 대칭이다:
  - **PRIMARY(실측)** — FLAT: 최상위 `type == "turn.completed"` 이면 `true`.
    ```
    {"type":"turn.completed","usage":{"input_tokens":14976,"cached_input_tokens":4480,"output_tokens":5,"reasoning_output_tokens":0}}
    ```
  - **FALLBACK** — 래핑: 최상위 `type == "event"` 이고 `event.type == "turn.completed"` 이면 `true`.
  그 외 이벤트 라인이거나 JSON 파싱 실패 시 `false`를 반환한다(panic 없음). `usage` 필드의 존재 여부를 판정 조건으로 삼지 않는다(있으면 참고, 없어도 `true`).
- **REQ-AI9-011** (Event-Driven) **[NEW]**: **WHEN** codex 릴레이 스레드가 stdout에서 `parse_codex_agent_message`가 `Some(text)`를 반환하는 라인을 수신하면, **the system shall** 해당 `text` 전체를 `ai://chunk` payload(`{requestId, text}`)로 **정확히 1회** emit한다. 토큰 단위 분할·청킹 금지 — codex의 `agent_message`는 완성본을 한 번에 보낸다(실측).
- **REQ-AI9-012** (Event-Driven) **[NEW]**: **WHEN** codex 릴레이가 `parse_codex_turn_completed`가 `true`인 라인을 수신하면, **the system shall** `ai://done` payload(`{requestId, result, truncated}`)을 발행한다. `result`는 가장 마지막으로 수신한 `agent_message` 본문(앞서 emit한 `ai://chunk`의 text와 동일). `truncated`는 기존 `assemble.truncated`(mod.rs:166) 값을 전달한다.
- **REQ-AI9-013** (Unwanted) **[NEW]**: **IF** codex stdout이 `agent_message`도 `turn.completed`도 없는 채 EOF에 도달하면, **then** the system shall 기존 `decide_outcome(final_result, stderr, cancelled, saw_stream_output)`(claude_cli.rs:145-165) 경로로 폴백한다. `final_result`는 `None`, `saw_stream_output`은 "stdout에 JSON 라인이 하나라도 왔는지"로 정의한다 — 이 경우 `parse` 또는 `other` kind의 `ai://error`가 발행된다(새 오류 종류·새 분기 금지).
- **REQ-AI9-013a** (Event-Driven) **[NEW]**: **WHEN** codex 릴레이가 `ai://chunk`(REQ-AI9-011)를 1회 emit한 이후 `turn.completed` 라인 수신 없이 stdout이 EOF하면, **the system shall** 마지막으로 수신한 `agent_message` 본문을 `result`로 하는 `ai://done` payload(`{requestId, result, truncated}`)을 발행한다(이미 사용자에게 본문이 전달된 상태이므로 `parse` 폴백 대신 정상 종료 처리). `truncated`는 기존 `assemble.truncated` 값을 전달한다. (참고: 본 REQ는 `chunk`를 1회라도 emit 한 케이스를 담당하며, `chunk` 0회인 폴백은 REQ-AI9-013이 담당한다.) 단, `claim_terminal` 단일 발행(SPEC-AI-006 REQ-AI6-006)을 선점한 경우에만 emit한다.
- **REQ-AI9-014** (Ubiquitous) **[NEW]**: The system **shall** codex stderr을 기존 `classify_stderr(stderr)`(stream.rs:78-126)로 분류한다. codex 전용 stderr 분류 로직·새 marker 상수를 중복 정의하지 않는다(단, `classify_stderr` 자체는 변경하지 않는다).

### 모듈 4 — codex 감지 (resolve_codex_binary, ~/.codex/auth.json 로그인)

- **REQ-AI9-015** (Ubiquitous) **[NEW]**: The system **shall** `resolve_codex_binary() -> Option<PathBuf>` 함수를 `detect.rs`에 제공한다. PATH의 각 디렉토리 + `codex`(Windows는 `codex.exe`) 후보를 먼저 만들고, 이어서 플랫폼 표준 설치 위치(macOS: `/opt/homebrew/bin/codex`, `/usr/local/bin/codex`, `~/.local/bin/codex`; Windows: `~/.local/bin/codex.exe`, `%APPDATA%\npm\codex.cmd`) 후보를 추가한다. 첫 번째로 존재하는 경로를 반환한다(`resolve_claude_binary` detect.rs:226-248과 동일 구조, 별도 함수). nvm 후보나 로그인셸 프로브는 v1에서 생략한다(별도 REQ로 확장 가능).
- **REQ-AI9-016** (Ubiquitous) **[NEW]**: The system **shall** `is_codex_logged_in(home: &Path) -> bool` 순수 함수를 제공한다. `home.join(".codex").join("auth.json")` 경로가 존재하면 `true`, 그 외 `false`를 반환한다(파일 내용 파싱은 v1에서 요구하지 않는다 — 존재 사실만). claude의 `is_logged_in`(detect.rs:99-107)과 대칭.
- **REQ-AI9-017** (Ubiquitous) **[NEW]**: The system **shall** `detect_codex() -> ProviderStatus`를 제공한다. `codex --version`(또는 `codex --help`의 버전 출력)이 성공하면 `installed=true` + 파싱된 version, 실패하면 `installed=false` + `version=None`으로 `ProviderStatus { id: "codex", installed, version, logged_in }`를 반환한다(`detect_claude` detect.rs:262-288과 동일 구조). `logged_in`은 `installed && is_codex_logged_in(home)`. 미설치 환경에서도 panic 없이 `id="codex"` 상태를 반환한다.

### 모듈 5 — 모델 매핑 (provider별 테이블, 기존 AiModel 재사용)

- **REQ-AI9-018** (Ubiquitous) **[NEW]**: The system **shall** codex의 `--model` 인자 값을 `AiModel::Haiku → "gpt-5.5"`, `AiModel::Sonnet → "gpt-5.5"`(v1 일단 동일)로 매핑한다. 매핑 로직은 `build_codex_args`(REQ-AI9-004) 내부 또는 `CodexProvider` 메서드로 중앙화한다(분산 금지). "Model Mapping" 표(아래) 참조.
- **REQ-AI9-019** (Unwanted) **[NEW]**: **IF** 코드가 `AiModel` enum의 변형(variant) 이름을 바꾸거나 신규 variant 추가를 시도하면, **then** the system **shall** 이를 거부한다 — `Haiku`/`Sonnet` 이름은 claude 특정 값처럼 보이나 본 SPEC에서는 **"기본/고급 티어" 의사 이름**으로 재해석해 그대로 유지한다. 프론트의 `aiAdvancedModel` 토글(uiStore)·`ipc.ts`의 `model?: 'haiku'|'sonnet'` 계약이 이 enum에 묶여 있어 변경하면 연쇄 파급이 발생한다(회귀 스냅샷 어서션으로 검증).
- **REQ-AI9-020** (Unwanted) **[MODIFY]** *(v0.0.3 개정 — 결함 2로 범위 축소)*: **IF** 코드가 (a) provider별 개별 설정 화면·탭, (b) 편집 화면 상단/하단의 "codex 사용 중" 상시 인디케이터, (c) `ai_detect_providers` 결과가 아닌 하드코딩된 provider 목록으로 구동되는 UI, (d) 설정 다이얼로그의 AI 도구 섹션 **외부**에 provider 선택 컨트롤을 도입하려 시도하면, **then** the system **shall** 이를 거부한다. 허용되는 유일한 provider 선택 표면은 설정 다이얼로그의 `AI 도구` 섹션에 렌더되는 대등 행 목록(REQ-AI9-026~032)이며, 그 외 provider 선택은 `providerId` 오버라이드(REQ-AI9-003)로만 이뤄진다. (v0.0.1~0.0.2의 "프론트 provider 선택 UI 전면 금지"는 본 개정으로 폐기 — 대등 행 UI는 결함 2 수정으로 범위에 편입되었다.)

### 모듈 6 — 무손상/하위호환 (claude 경로·IPC·프롬프트 조립 무변경, 회귀 가드)

- **REQ-AI9-021** (Ubiquitous) **[EXISTING]**: The system **shall** 기존 IPC 계약 `AiRequestArgs.providerId`(`provider_id: Option<String>`, mod.rs:89, `#[serde(default)]`)를 재사용한다. 새 IPC 필드·새 이벤트 타입을 추가하지 않는다. `ai://chunk`/`ai://done`/`ai://error` payload 스키마(claude_cli.rs:27-54)도 무변경.
- **REQ-AI9-022** (Unwanted) **[REGRESSION_GUARD]**: **IF** codex 통합 코드가 기존 claude 인자 조립(`build_claude_args(model, system_prompt, user_prompt)`, claude_cli.rs:81-98)을 수정하려 시도하면, **then** the system **shall** 이를 거부한다 — 동일 `(AiModel, &str, &str)` 입력에 대해 산출 `Vec<String>`이 **변경 전과 바이트 단위로 동일**해야 한다(Rust `#[cfg(test)]` 회귀 스냅샷 어서션, `claude_cli.rs` 내 `#[cfg(test)]`에 신규 추가).
- **REQ-AI9-023** (Unwanted) **[REGRESSION_GUARD]**: **IF** codex 통합 코드가 기존 claude 파서(`parse_text_delta`(stream.rs:40-55)·`parse_final_result`(stream.rs:62-72))를 수정하려 시도하면, **then** the system **shall** 이를 거부한다 — 동일 JSON 라인 입력에 대해 동일 `Option<String>`을 반환해야 한다(회귀 단위 테스트로 검증).
- **REQ-AI9-024** (Unwanted) **[REGRESSION_GUARD]**: **IF** codex 통합 코드가 기존 claude 프롬프트 조립(`prompt.rs`의 `build_inline_prompt`/`build_section_prompt`/`build_continue_prompt_with_length`)을 수정하려 시도하면, **then** the system **shall** 이를 거부한다 — 동일 `(feature, presetKind, customInstruction, selection, contextBefore, contextAfter, outline, diagramType)` 입력에 대해 산출 `(system_prompt, user_prompt)` 쌍이 **변경 전과 바이트 단위로 동일**해야 한다(SPEC-AI-008 REQ-AI-008-025 회귀 가드와 동일 계약, 회귀 스냅샷 테스트로 검증).
- **REQ-AI9-025** (Unwanted) **[GUARD]**: **IF** 자동 감지 로직이 `claude`가 설치+로그인된 환경에서 `codex`를 기본 프로바이더로 선택하려 시도하면, **then** the system **shall** 이를 거부하고 `claude`를 기본으로 둔다(REQ-AI9-002 우선순위 계약). `claude` 미설치 환경에서만 `codex`가 폴백 기본이다(`first_available()` 단위 테스트로 검증).

### 모듈 7 — 대등한 provider 선택 UI (v0.0.3 신설, 결함 2)

> **결함 배경**: `SettingsModal.tsx`가 Claude 전용 상태 블록(리터럴 "Claude Code" + 배지·버전·온보딩 문구, ~180/196/209행)을 하드코딩하고 codex는 `AiProviderSelect` 드롭다운(~358행)의 옵션으로만 존재해, claude=주 도구 / codex=부가 설정처럼 보였다. `deriveConnectionState`도 섹션 전체를 단일 "유효 provider"로 환원한다. 본 모듈은 **두 도구를 형태가 동일한 행으로 대등하게 제시**한다.
>
> 목표 형태(사용자 확정 — 드롭다운 유지안·세그먼트 토글안은 명시적으로 기각됨):
> ```
> AI 도구
>
> ( ) Claude Code      ✅ 사용 가능 (v2.1.218)
> (•) codex            ✅ 사용 가능 (v0.144.1)
>
> ☑ AI 기능 사용
> ```

- **REQ-AI9-026** (Ubiquitous) **[MODIFY]**: The system **shall** 설정 다이얼로그의 `AI 도구` 섹션에서 감지된 각 provider를 **형태가 동일한 1행**으로 렌더한다. 각 행은 정확히 다음 4요소를 같은 순서로 포함한다: (1) 단일 선택 컨트롤(radio, 동일 `name` 그룹), (2) provider 표시명, (3) 상태 배지, (4) 버전 문자열(`version`이 `None`이면 버전 요소를 생략하되 나머지 3요소는 유지). 특정 provider만 더 크거나·더 앞에 고정되거나·추가 문구를 갖는 비대칭 렌더를 허용하지 않는다.
- **REQ-AI9-027** (Ubiquitous) **[MODIFY]**: The system **shall** 각 행의 상태를 **그 provider 자신의 `ProviderStatus`만으로** 파생한다(다른 provider나 "유효 provider" 개념에 의존 금지). 파생 규칙은 다음 표로 고정한다:
  | `installed` | `loggedIn` | 배지 문구 | 선택 가능 |
  |-------------|------------|-----------|-----------|
  | true        | true       | `사용 가능` | 예 |
  | true        | false      | `로그인 필요` | 아니오 |
  | false       | (무시)     | `미설치` | 아니오 |
  이 파생은 순수 함수로 제공되어 UI 렌더 없이 단위 테스트 가능해야 한다.
- **REQ-AI9-028** (Unwanted) **[MODIFY]**: **IF** 사용자가 `installed == false` 이거나 `loggedIn == false` 인 provider 행을 선택하려 시도하면, **then** the system **shall** 선택을 거부하고(해당 행의 선택 컨트롤이 `disabled`), 선택 불가 사유(REQ-AI9-027의 `로그인 필요` / `미설치`)를 **그 행 안에서 인라인으로** 표시한다. 사유를 툴팁·별도 다이얼로그·섹션 하단 각주로 옮기지 않는다(행 단위 자족성).
- **REQ-AI9-029** (Ubiquitous) **[MODIFY]**: The system **shall** 행 목록을 `ai_detect_providers` IPC가 반환한 **배열 순서 그대로**(백엔드 registry 등록 순서 = claude, codex) 순회하며 렌더한다. `providers.find(p => p.id === 'claude')` 류의 id 하드코딩 조회로 행을 구성하지 않는다. 결과적으로 registry에 3번째 provider가 추가되면 **`SettingsModal.tsx` 수정 없이** 3번째 행이 렌더되어야 한다.
- **REQ-AI9-030** (Ubiquitous) **[MODIFY]**: The system **shall** 기존 `AiProviderSelect` 드롭다운을 REQ-AI9-026의 행 목록으로 **대체**한다(드롭다운은 제거되고 두 표면이 공존하지 않는다). 단, 표현 계층만 바뀌며 다음은 **무변경**이다: (a) 영속화 키 `aiSelectedProvider`(`src/store/uiStore.ts`, 값 도메인 `'auto' | 'claude' | 'codex'`), (b) IPC 계약 `providerId?: string`(`src/lib/tauri/ipc.ts`) ↔ Rust `provider_id: Option<String>`(mod.rs:89), (c) `resolveProviderId` 매핑(`src/test/aiProviderId.test.ts`가 검증하는 계약).
- **REQ-AI9-031** (Unwanted) **[MODIFY]**: **IF** 조직 정책 kill-switch(`aiPolicyStatus().disabled == true`)가 활성이면, **then** the system **shall** 모든 provider 행의 선택 컨트롤을 `disabled` 처리하고 자물쇠 표기(🔒)를 렌더한다 — `AdvancedModelToggle`/`AiEnabledToggle`의 기존 정책 잠금 관례(SPEC-AI-005 REQ-AI5-005)와 동일하다. 정책 잠금 상태에서 provider 선택이 변경되는 경로가 존재해서는 안 된다.
- **REQ-AI9-032** (Event-Driven) **[MODIFY]**: **WHEN** `installed == true && loggedIn == false` 인 provider 행이 렌더되면, **the system shall** 해당 행에서 기존 온보딩 진입점(`onStartOnboarding` → `OnboardingWizard`)에 도달 가능한 컨트롤을 함께 노출한다. 드롭다운 제거로 온보딩 경로가 소실되어서는 안 된다(회귀 방지).

### 모듈 8 — AI 카드 생명주기: 파일 전환 정리 + 종결 phase 닫기 (v0.0.4 신설, 결함 3)

> **결함 배경(소스 검증)**: AI 요청 실패 후 오류 카드("잠시 문제가 있었어요. 다시 시도해주세요." + [다시 시도])가 화면에 남고, **다른 파일을 열어도 사라지지 않은 채** 새 문서의 무관한 위치에 다시 렌더된다. 원인은 둘이다.
> - **(3a) 파일 전환 정리 경로 부재**: 카드 레지스트리(`registerCardController`/`removeCardController`/`clearCardRegistry`, `ai-suggestion-card.ts:759-789`)의 유일한 일괄 정리 경로는 AI 토글 ON→OFF 전이(`aiOffEffects.ts:36`, `initAiToggleEffects` → `AppLayout.tsx:84` 1회 등록)뿐이다. 파일 열기는 단일 진입점 `useFileSystem.openFile`(`useFileSystem.ts:135`)로 수렴하지만 대응하는 정리 훅이 없다. 살아남은 카드의 `range`는 **이전 문서**의 위치이므로 새 문서의 임의 오프셋에 앵커된다.
> - **(3b) 종결 phase에 닫기 컨트롤 부재**: `error` phase 렌더(`ai-suggestion-card.ts:329-353`)는 `login`→[연결 안내 보기], `network`→버튼 없음, `other`→[다시 시도]만 제공한다. [무시](`callbacks.onCancel`)는 `intruded` phase 전용이고 `empty`/`cancelled-by-new`/`stale`도 버튼이 없다. 사용자가 수동으로 카드를 치울 방법이 아예 없다.

- **REQ-AI9-033** (Event-Driven) **[NEW]**: **WHEN** 활성 문서가 다른 파일로 바뀌면(`fileStore.currentFile`이 이전 값과 다른 non-null 값으로 전이), **the system shall** 다음 3동작을 **정확히 1회** 수행한다: (1) in-flight AI 요청이 있으면 `aiCancel(requestId)` + `aiStore.cancelRequest()`로 취소, (2) 활성 고스트 텍스트가 있으면 `clearGhostEffect`로 제거, (3) 카드 레지스트리에 컨트롤러가 하나라도 있으면 `clearCardRegistry()`로 비움. 이는 기존 AI OFF 전이 정리(SPEC-AI-005 REQ-AI5-011, `runAiOffCleanup`)와 **대칭인 폴백**이며, 동작 집합·순서가 동일하다.
- **REQ-AI9-034** (Ubiquitous) **[NEW]**: The system **shall** REQ-AI9-033의 정리를 **독립 effects 모듈**이 담당하게 한다. 이 모듈은 (a) `fileStore.subscribe`로 `currentFile` 전이를 관찰하고, (b) `AppLayout`이 마운트 시 **1회** 등록하며 언마운트 시 반환된 해제 함수를 호출하고, (c) 구독 콜백 내부에서 `useFileStore`를 다시 쓰지 않는다(재진입 없음 — `aiOffEffects.ts:1-7`에 문서화된 순환 방지 속성과 동일 계약). 정리 로직을 `useFileSystem.openFile` 내부·`uiStore`·`fileStore` 액션 내부에 결합하지 않는다.
- **REQ-AI9-035** (Unwanted) **[NEW]**: **IF** 파일 전환 정리(REQ-AI9-033)가 문서 텍스트를 삽입·삭제·치환하려 시도하면, **then** the system **shall** 이를 거부한다 — 정리는 **삽입 전(pre-insertion) 산출물만** 폐기한다(고스트는 뷰 레이어 전용 `StateEffect`, 카드 레지스트리는 아직 문서에 삽입되지 않은 제안). SPEC-AI-001 REQ-AI-033 무손상 원칙 및 SPEC-AI-005 REQ-AI5-012와 동일한 HARD 불변이며, 새로 연 문서·이전 문서 어느 쪽의 본문도 바뀌어서는 안 된다.
- **REQ-AI9-036** (Ubiquitous) **[MODIFY]**: The system **shall** `error` phase 카드에 **모든 `errorKind`(`login`/`network`/`other`)에 대해** 명시적 닫기 컨트롤(라벨 `닫기`)을 렌더한다. 닫기는 기존 액션 버튼([연결 안내 보기] / [다시 시도])과 **병존**하며(대체하지 않음), 클릭 시 해당 카드를 레지스트리에서 제거해 화면에서 사라지게 한다. `network` kind처럼 액션 버튼이 없는 경우에도 닫기는 반드시 존재한다.
- **REQ-AI9-037** (Ubiquitous) **[MODIFY]**: The system **shall** REQ-AI9-036의 닫기 컨트롤을 나머지 **버튼 없는 종결 phase** — `empty`, `cancelled-by-new`, `stale` — 에도 동일한 라벨·동작으로 렌더한다. 종결 phase 간에 "닫을 수 있는 것과 없는 것"이 갈리지 않는다(일관성 계약, 근거는 Design Notes).
- **REQ-AI9-038** (Unwanted) **[NEW]**: **IF** 닫기 컨트롤(REQ-AI9-036/037)이 (a) 문서 텍스트를 변경하거나, (b) 진행 중(`streaming`) phase 카드에 렌더되거나, (c) 재시도·재요청 같은 부가 동작을 함께 수행하려 시도하면, **then** the system **shall** 이를 거부한다 — 닫기는 **레지스트리 제거 1가지 부수효과만** 갖는다. `streaming` phase는 이미 [✕ 취소](`ai-suggestion-card.ts:315-317`)를 제공하므로 닫기를 중복 렌더하지 않는다.

### 모듈 9 — 고스트 종결-빈 결과 상태 (v0.0.5 신설, 결함 4)

> **결함 배경(재현 완료)**: `src/test/aiGhostEmptyDone.repro.test.ts` 가 `startGhostEffect` → `completeRequest('', false)` 를 구동해 `placeholder = ✨ 작성 중…` 과 `buttons = ['✓ 넣기','✕ 지우기']` 를 **동시에** 관측했다. 기전은 3개 지점이 맞물린 결과다.
> - `src/store/aiStore.ts:65-72` `reduceCompleteRequest` 가 `streamBuffer` 를 최종 `result` 로 덮어써 `result === ''` 이면 버퍼가 비워진다.
> - `ai-ghost-text.ts:848-849` `ghostStoreBridge` 가 그 버퍼를 `setGhostTextEffect.of('')` + `setGhostStatusEffect.of('done')` 로 그대로 미러링한다.
> - `ai-ghost-text.ts:257-269` `ghostDecorations` 가 `text === ''` 를 "첫 청크 미도착"으로 해석해 `GhostPlaceholderWidget` 을 렌더하고, 동시에 `status === 'done'` 이라 `GhostControlsWidget` 이 [✓ 넣기]/[✕ 지우기]를 렌더한다.
>
> 이후 이벤트가 없어 상태는 영구 고착되고, `confirmGhostCommand`(`ai-ghost-text.ts:468-470`)의 `!ghost.text` 가드 때문에 **[✓ 넣기]는 조용한 no-op** 이다. 빈 `result` 는 두 프로덕션 경로에서 실제로 발생한다 — codex(`codex_cli.rs` `relay_codex_process` 의 `last_message.clone().unwrap_or_default()`)와 claude(`parse_final_result` 빈 문자열). 카드 경로는 동일 상황을 이미 `empty` phase(`ai-suggestion-card.ts:81-88`)로 올바르게 처리하므로, 고스트 경로의 부재는 **설계 의도가 아니라 누락**이다.
>
> 목표 형태(사용자 확정 — 무통보 자동 제거안·[다시 요청] 추가안은 명시적으로 기각됨):
> ```
> Question: 15 나누기 3 더하기 20은 얼마인가요?
>  ℹ 더 쓸 내용을 찾지 못했어요  [✕ 닫기]
> ```

- **REQ-AI9-039** (Event-Driven) **[MODIFY]**: **WHEN** 고스트 경로 요청(`feature === 'section-fill'` — `ai-ghost-text.ts:836-840` 의 `@MX:NOTE` 가 규정한 대로 섹션 채우기·문서 끝 이어쓰기·자유 위치 이어쓰기 전부를 포괄)이 종결 상태(`requestState === 'done'`)로 전이하면서 최종 텍스트가 REQ-AI9-040의 "실질적 빈 값"이면, **the system shall** 활성 고스트를 **terminal-empty** 상태로 렌더한다. 이 상태는 정확히 다음 2요소만 포함한다: (1) 안내 문구 `더 쓸 내용을 찾지 못했어요`(정보 표기 `ℹ` 병기 허용), (2) 닫기 컨트롤 `✕ 닫기` **정확히 1개**. 닫기는 기존 소멸 경로(`dismissGhostCommand`)와 동일한 결과(고스트 제거)를 낳는다.
- **REQ-AI9-040** (Ubiquitous) **[MODIFY]**: The system **shall** "실질적 빈 값"을 **최종 텍스트의 `trim()` 결과가 빈 문자열인 경우**로 정의한다. 이 정의는 카드 경로의 기존 헬퍼 `isEmptyOrIdentical`(`ai-suggestion-card.ts:72-75`)의 **빈/공백-only 판정부와 동일 의미론**이며, 신규 판정 규칙을 발명하지 않는다. 단, 같은 헬퍼의 "원문과 동일(identical)" 판정은 고스트 경로에 적용하지 않는다 — 고스트는 삽입 앵커만 가질 뿐 대체 대상 원문이 없기 때문이다.
- **REQ-AI9-041** (Unwanted) **[MODIFY]**: **IF** terminal-empty 상태(REQ-AI9-039)에서 코드가 (a) 대기 플레이스홀더 `GhostPlaceholderWidget`(`.mdedit-ai-ghost-placeholder`, "✨ 작성 중…")을 렌더하거나, (b) 확정 컨트롤 [✓ 넣기]를 렌더하려 시도하면, **then** the system **shall** 이를 거부한다 — (a)는 종결된 요청을 진행 중으로 오인시키고, (b)는 `confirmGhostCommand` 의 `!ghost.text` 가드로 인해 클릭해도 아무 일도 일어나지 않는 **조용한 no-op** 이어서 사용자를 오도한다. 대기 안내(`WAIT_NOTICE_TEXT`) 역시 이 상태에서 렌더되지 않는다.
- **REQ-AI9-042** (Unwanted) **[NEW]**: **IF** terminal-empty 렌더 또는 그 닫기 동작이 (a) 문서 텍스트를 삽입·삭제·치환하거나, (b) 사용자 조작 없이 고스트를 자동 소멸시키려 시도하면, **then** the system **shall** 이를 거부한다. (a)는 SPEC-AI-001 REQ-AI-033 무손상 불변이며(고스트는 뷰 레이어 전용 `StateEffect`), (b)는 무통보 취소 금지 원칙(SPEC-AI-006 REQ-AI6-006 계열, P7)에 따라 **사용자가 결과를 인지한 뒤 직접 치우는 것**이 유일한 종결 경로임을 보장한다.
- **REQ-AI9-043** (Ubiquitous) **[EXISTING]**: The system **shall** 백엔드 릴레이 계약을 **무변경**으로 유지한다 — `relay_codex_process`(`src-tauri/src/ai/codex_cli.rs`)가 `turn.completed` 시 `last_message.clone().unwrap_or_default()`로 산출한 **빈 `result` 를 담은 `ai://done` emit은 여전히 적법**하며, claude의 `parse_final_result` 빈 문자열 경로도 동일하다. 빈 결과를 백엔드에서 억제하거나 오류(`ai://error`)로 전환하는 수정을 도입하지 않는다. 빈 결과의 사용자 표현은 **프론트 렌더 계층 단일 지점**이 담당하며, 이로써 codex와 claude가 동일한 동작 경로를 공유한다.
- **REQ-AI9-044** (Unwanted) **[MODIFY]**: **IF** 코드가 terminal-empty 안내에 (a) [다시 요청]/재시도 컨트롤을 추가하거나, (b) 닫기 외의 추가 컨트롤을 렌더하려 시도하면, **then** the system **shall** 이를 거부한다 — 고스트 경로는 이미 별도의 ↻ 재요청 어포던스(`reRequestGhost`, `ai-ghost-text.ts:439`)를 제공하며, `ai-ghost-text.ts:347` 에 문서화된 클래스 분리 제약(기존 테스트가 **done(비어있지 않음) 상태**의 `.cm-ai-ghost-btn` 개수를 정확히 2개로 단언)이 유지되어야 한다. terminal-empty 상태에서는 닫기 컨트롤이 **정확히 1개**만 존재하므로 done 상태의 "정확히 2개" 단언과 충돌하지 않는다(두 상태는 서로 배타적).

### 모듈 10 — 고급 모델 토글의 provider 대등성 (v0.0.6 신설, 결함 5)

> **결함 배경(소스 대조 확인)**: 프론트의 `resolveModel`(`SettingsModal.tsx:46-48`, `ai-selection-toolbar.ts:168-170`)이 토글을 `'sonnet' | 'haiku'` 로 환원 → IPC `model` → `AiModel::from_opt`(`provider.rs:28-34`) → `Haiku | Sonnet`. 그런데 codex 경로에서 `codex_model_arg`(`codex_cli.rs:41-45`)가 **두 티어를 모두 `"gpt-5.5"` 로** 매핑하고 `build_codex_args`(`codex_cli.rs:60-80`)가 `-c model_reasoning_effort="medium"` 을 하드코딩한다. 따라서 codex 선택 상태에서 고급 토글은 인자 벡터를 **1바이트도 바꾸지 않는다**(죽은 토글). 라벨도 `고급 모델 사용 (sonnet — …)` / `aria-label="고급 모델 사용 (sonnet)"`(`SettingsModal.tsx:286,291`)로 codex 사용자에게 틀린 모델명을 말한다.
>
> **claude 쪽 비대칭**: claude 에서 codex 의 reasoning effort 에 대응하는 것은 모델명이 아니라 **사고 예산**이다 — `spawn_claude`(`claude_cli.rs:179`)의 `.env("MAX_THINKING_TOKENS", "0")` 이 **두 티어 모두에** 무조건 걸려 claude 의 추론이 전면 비활성 상태다.
>
> 목표 동작(사용자 확정 — 재검토 금지):
>
> | 티어 | claude | codex |
> |------|--------|-------|
> | 기본 (`AiModel::Haiku`) | `--model haiku` + `MAX_THINKING_TOKENS=0`(현행 유지) | `--model gpt-5.5` + `model_reasoning_effort="medium"`(현행 유지) |
> | 고급 (`AiModel::Sonnet`) | `--model sonnet` + **`MAX_THINKING_TOKENS` 미설정**(CLI 기본값 위임) | `--model gpt-5.5` + `model_reasoning_effort="high"` |
>
> **라벨 설계 *(v0.0.7 개정 — 백엔드 공급 채택)***: 라벨의 고급 티어 표기는 **백엔드가 내려준다** — `ProviderStatus.advanced_model_label`(IPC `advancedModelLabel`, REQ-AI9-051)이 claude → `sonnet`, codex → `gpt-5.5 · 높은 추론` 을 싣고, 그 값은 실제 인자를 만드는 **동일한 중앙 매핑 함수**에서 파생된다(REQ-AI9-052 HARD). 프론트는 받은 문자열을 그대로 감싸 렌더하고(REQ-AI9-048), 필드가 비어 있을 때만 표시명 기반으로 폴백한다(REQ-AI9-053). ~~(v0.0.6: 프론트가 `providerDisplayName` 만으로 `{표시명} 고급 티어` 를 구성)~~ 는 폴백 경로로 강등되었다.

- **REQ-AI9-045** (Ubiquitous) **[MODIFY]**: The system **shall** codex 의 추론 강도를 `AiModel` 에서 파생한다 — `AiModel::Haiku → "medium"`, `AiModel::Sonnet → "high"`. 파생은 `codex_model_arg`(`codex_cli.rs:41-45`)와 형제인 **단일 순수 함수**(예: `codex_reasoning_effort(model: AiModel) -> &'static str`)에 중앙화하고 분산을 금지한다(Design Notes "모델 매핑 중앙화"와 동일 계약). `build_codex_args`(REQ-AI9-004)는 `-c` 다음 원소를 `format!("model_reasoning_effort=\"{}\"", codex_reasoning_effort(model))` 로 조립한다. **인자 벡터의 원소 개수(15)·순서·그 외 모든 값은 무변경**이며, 두 티어의 산출 벡터는 **`model_reasoning_effort` 값이 들어있는 원소 정확히 1개**에서만 달라진다. `--model` 값은 두 티어 모두 여전히 `"gpt-5.5"` 다(사전 합의 §2 모델 매핑 무변경).
- **REQ-AI9-046** (Ubiquitous) **[MODIFY]**: The system **shall** claude 의 사고 예산 환경변수를 `AiModel` 에서 파생한다 — `AiModel::Haiku` 이면 `MAX_THINKING_TOKENS=0` 을 설정하고, `AiModel::Sonnet` 이면 **환경변수를 아예 설정하지 않는다**(claude CLI 자체 기본값에 위임). 파생은 `Tauri` 런타임 없이 단위 테스트 가능한 **순수 함수**(예: `claude_thinking_env(model: AiModel) -> Option<(&'static str, &'static str)>`, 고급 티어에서 `None`)로 분리하고, `spawn_claude`(`claude_cli.rs:170-185`)는 그 결과가 `Some((k, v))` 일 때만 `.env(k, v)` 를 호출한다. `--model` 값은 기존 `AiModel::as_arg()`(`provider.rs:21-26`)가 그대로 `haiku`/`sonnet` 을 산출하므로 `build_claude_args` 는 **무변경**이다(REQ-AI9-022 회귀 가드 유지 — 본 REQ 의 변경 표면은 `spawn_claude` 의 env 배선과 그 시그니처뿐이다).
- **REQ-AI9-047** (Unwanted) **[NEW]**: **IF** 코드가 고급 티어에 대해 `MAX_THINKING_TOKENS` 를 **숫자 리터럴**(예: `"4000"`, `"8000"`)로 설정하려 시도하면, **then** the system **shall** 이를 거부한다 — 어떤 숫자를 골라도 출처가 없어 사후에 정당화·검증할 수 없는 매직 상수가 되고, claude CLI 의 권장 기본값이 버전업으로 바뀌어도 따라가지 못한다. "미설정 = CLI 기본값"은 자기문서적이며 버전 안정적이다. 소스 전체에서 `MAX_THINKING_TOKENS` 에 결합되는 값 리터럴은 기본 티어의 `"0"` **하나뿐**이어야 한다.
- **REQ-AI9-048** (Ubiquitous) **[MODIFY]** *(v0.0.7 개정 — 백엔드 공급 라벨 채택)*: The system **shall** `AdvancedModelToggle`(`SettingsModal.tsx:269-293`)의 표시 라벨과 `aria-label` 을 **현재 선택된 provider 의 `advancedModelLabel`**(REQ-AI9-051)에서 렌더한다. 유효 provider 판정은 `uiStore.aiSelectedProvider`(`'auto' | 'claude' | 'codex'`)를 입력으로 하며, 값이 `'auto'` 이면 감지 결과 배열에서 `deriveProviderRowState(p).selectable === true` 인 **첫 provider**(= 백엔드 `first_available()` 우선순위와 동일 규칙, REQ-AI9-002)를 유효 provider 로 본다. 렌더 형태는 `고급 모델 사용 ({advancedModelLabel} — 더 정확, 더 느림)` 이고 `aria-label` 은 `고급 모델 사용 ({advancedModelLabel})` 로 **동일 소스**를 쓴다 — 목표 결과: codex 선택 시 `고급 모델 사용 (gpt-5.5 · 높은 추론 — 더 정확, 더 느림)`, claude 선택 시 `고급 모델 사용 (sonnet — 더 정확, 더 느림)`. 필드가 부재·빈 값인 경우의 동작은 REQ-AI9-053이 규정한다. 라벨·`aria-label` 어디에도 프론트 소스에서 유래한 리터럴 `"sonnet"` 이 남아 있어서는 안 된다. ~~(v0.0.6: `providerDisplayName` 기반 `{표시명} 고급 티어` 형태)~~ 는 **폴백 경로로 강등**되었다.
- **REQ-AI9-049** (Unwanted) **[MODIFY]** *(v0.0.7 개정 — 복제 금지 대상 확장)*: **IF** 코드가 (a) 프론트에 provider id → 모델명(`"sonnet"`, `"gpt-5.5"` 등) 리터럴 테이블을 도입하거나, (b) **백엔드에 `advancedModelLabel` 값을 만드는 두 번째 상수·두 번째 매핑**(예: `codex_model_arg`/`codex_reasoning_effort`/`AiModel::as_arg` 를 호출하지 않고 같은 문자열을 다시 적어둔 `const CODEX_ADVANCED_LABEL = "gpt-5.5 · 높은 추론"`)을 도입하거나, (c) 폴백 없는(미등록 id 에서 `undefined`·빈 문자열·throw 로 귀결되는) provider 별 리터럴 테이블로 라벨을 구성하려 시도하면, **then** the system **shall** 이를 거부한다. 근거: (a)·(b) 모델명·추론 강도 매핑의 단일 소스는 REQ-AI9-052가 지정한 중앙 함수뿐이며, 어디든 복제되는 순간 매핑 개정 시 라벨이 **조용히 거짓말**을 시작한다 — 결함 5가 정확히 그 형태다(라벨은 `sonnet`, 실제는 `gpt-5.5`). 복제본은 프론트에 있든 Rust 에 있든 동일하게 위험하다. (c) 폴백 없는 테이블은 REQ-AI9-029(3번째 provider 무수정 렌더)를 깨뜨린다.
- **REQ-AI9-050** (Unwanted) **[REGRESSION_GUARD]** *(v0.0.7 (d) 정정)*: **IF** 본 모듈의 구현이 **기본 티어(`AiModel::Haiku`)** 의 관측 가능한 동작을 바꾸려 시도하면, **then** the system **shall** 이를 거부한다 — 구체적으로 (a) `build_codex_args(Haiku, …)` 산출 벡터가 개정 전과 **바이트 동등**(15원소, `--model gpt-5.5`, `model_reasoning_effort="medium"`), (b) `build_claude_args(Haiku|Sonnet, …)` 산출 벡터가 개정 전과 **바이트 동등**(REQ-AI9-022 계약 연장), (c) `spawn_claude` 가 기본 티어에서 여전히 `MAX_THINKING_TOKENS=0` 을 설정, (d) *(v0.0.7 정정)* `AiRequestArgs` IPC 계약(`model?: 'haiku'|'sonnet'`, `providerId?`)·프론트 `resolveModel` 2곳의 반환 도메인·`AiModel` enum variant 이름 및 `as_arg`/`from_opt` 본문이 무변경이고, `src-tauri/src/ai/prompt.rs`·`src/store/uiStore.ts`·`src/components/editor/extensions/ai-selection-toolbar.ts` 의 diff 가 **0줄**이어야 한다. **`provider.rs` 의 `ProviderStatus` 와 `ipc.ts` 의 `AiProviderStatus` 는 REQ-AI9-051의 선택 필드 1개 추가에 한해 변경이 허용된다** — v0.0.6이 이 두 파일을 통째로 "diff 0줄"로 단언했던 것은 본 개정에서 **명시적으로 정정**된 것이며, 그 외 필드·타입은 여전히 무변경이다.
- **REQ-AI9-051** (Ubiquitous) **[MODIFY]** *(v0.0.7 신설)*: The system **shall** `ProviderStatus`(`src-tauri/src/ai/provider.rs:44-55`)에 고급 티어 표시 문자열 필드를 **정확히 다음 shape 로** 추가한다:
  - Rust: `pub advanced_model_label: Option<String>` — 기존 `version` 필드와 동일하게 `#[serde(skip_serializing_if = "Option::is_none")]` 를 붙인다(구조체의 `#[serde(rename_all = "camelCase")]` 에 의해 IPC 로는 `advancedModelLabel` 로 직렬화된다).
  - TypeScript: `src/lib/tauri/ipc.ts` 의 `AiProviderStatus`(259-264행)에 `advancedModelLabel?: string` 추가. 그 외 필드(`id`/`installed`/`version`/`loggedIn`)는 무변경.
  - 값의 형태: **짧은 표시용 한 줄 문자열**이며 (1) 괄호로 감싸지 않고, (2) `고급 모델 사용` 같은 문구 접두사를 포함하지 않고, (3) 마침표·후행 공백을 붙이지 않는다 — 감싸는 문장은 프론트가 만든다(REQ-AI9-048). 확정 값: claude → `sonnet`, codex → `gpt-5.5 · 높은 추론`.
  - 채움 위치: 각 어댑터의 `AiProvider::detect()`(`ClaudeProvider::detect` `claude_cli.rs:286-288`, `CodexProvider::detect`)가 `detect::detect_claude()`/`detect_codex()` 결과에 본 필드를 채워 반환한다. `detect.rs` 의 두 생성 지점(`detect.rs:282-287` 등)은 `None` 으로 두어 매핑 지식이 `detect.rs` 로 새지 않게 한다.
- **REQ-AI9-052** (Ubiquitous) **[NEW]** *(v0.0.7 신설, HARD 단일 소스)*: The system **shall** `advanced_model_label` 값을 **실제 인자를 조립하는 것과 동일한 중앙 매핑 함수**에서 파생한다 — codex 는 `codex_model_arg(AiModel::Sonnet)` 와 `codex_reasoning_effort(AiModel::Sonnet)`(REQ-AI9-045), claude 는 `AiModel::as_arg(AiModel::Sonnet)`(`provider.rs:21-26`). [HARD] 라벨 조립 코드에 `"sonnet"`·`"gpt-5.5"`·`"high"` 문자열 리터럴이 등장해서는 안 된다. codex 라벨은 `format!("{} · {}", codex_model_arg(Sonnet), codex_effort_display(codex_reasoning_effort(Sonnet)))` 형태로 조립하며, `codex_effort_display(effort: &str) -> &str` 는 **중앙 함수가 반환한 값 자체를 입력으로 받아** 사람이 읽는 표기로 옮긴다(`"high" → "높은 추론"`, `"medium" → "보통 추론"`, **그 외 알 수 없는 키는 원문 그대로 통과**). 이 통과 규칙이 핵심이다 — 향후 `codex_reasoning_effort` 가 새로운 키를 반환하면 라벨은 그 키를 날것으로 노출할 뿐 **"높은 추론" 이라고 거짓 주장하지 않는다**. 티어→문자열 매칭을 라벨 쪽에서 다시 수행하는 구현(예: `match model { Sonnet => "높은 추론" }`)은 REQ-AI9-049 (b) 위반이다.
- **REQ-AI9-053** (State-Driven) **[MODIFY]** *(v0.0.7 신설, 폴백)*: **WHILE** 유효 provider 의 `advancedModelLabel` 이 부재(`undefined`)·빈 문자열·공백만인 상태이면(3번째 provider 가 필드를 채우지 않았거나 구버전 백엔드와 조합된 경우), the system **shall** 라벨을 v0.0.6 형태 — `고급 모델 사용 ({providerDisplayName(id)} 고급 티어 — 더 정확, 더 느림)`, `aria-label` 은 `고급 모델 사용 ({providerDisplayName(id)} 고급 티어)` — 로 폴백한다. **WHILE** 유효 provider 자체가 없으면(모든 행이 선택 불가), the system **shall** provider 를 언급하지 않는 중립 문구 `고급 모델 사용 (더 정확, 더 느림)` 을 렌더한다. 어느 폴백 경로에서도 예외를 던지지 않고, `undefined`·`null` 문자열이 화면에 노출되지 않으며, 토글의 체크 상태·`setAiAdvancedModel` 배선·정책 잠금 `disabled`+🔒 관례는 영향을 받지 않는다.

## Model Mapping

아래 표는 REQ-AI9-018의 매핑 계약을 고정한다. `AiModel::Haiku`/`Sonnet`은 provider 무관 "기본/고급" 티어 의사 이름이며, 각 provider가 자체 문자열로 변환한다.

| Provider | `AiModel::Haiku` → `--model` 값 | `AiModel::Sonnet` → `--model` 값 | 매핑 소유 함수 |
|----------|----------------------------------|----------------------------------|----------------|
| claude   | `haiku`                          | `sonnet`                         | `AiModel::as_arg()` (provider.rs:21-26, 기존 무변경) |
| codex    | `gpt-5.5`                        | `gpt-5.5` (v1 동일)              | `build_codex_args` 내부 match (REQ-AI9-004, REQ-AI9-018) |

> codex의 양 티어가 동일한 `gpt-5.5`로 매핑되는 것은 v1 한정 결정이다(사전 합의). 이후 SPEC 개정으로 `gpt-5.5` 이외 모델을 도입할 때는 본 표와 REQ-AI9-018을 함께 개정한다.

### 추론 강도 매핑 *(v0.0.6 신설, REQ-AI9-045/046)*

위 표(`--model` 값)는 무변경이다. v0.0.6은 **직교하는 두 번째 차원**인 "추론 강도"를 티어에서 파생한다. 두 provider 는 서로 다른 메커니즘을 쓴다 — codex 는 `-c model_reasoning_effort` 설정 값, claude 는 `MAX_THINKING_TOKENS` 환경변수다.

| Provider | 기본(`AiModel::Haiku`) | 고급(`AiModel::Sonnet`) | 매핑 소유 함수 |
|----------|------------------------|--------------------------|----------------|
| claude   | env `MAX_THINKING_TOKENS=0` | env **미설정**(CLI 기본값) | `claude_thinking_env(model)`(REQ-AI9-046) |
| codex    | `model_reasoning_effort="medium"` | `model_reasoning_effort="high"` | `codex_reasoning_effort(model)`(REQ-AI9-045) |

> 고급 티어 claude 에 숫자 예산을 넣지 않는 이유는 REQ-AI9-047 및 Design Notes "고급 티어 사고 예산을 미설정으로 두는 이유" 참조.

## Design Notes / Future Considerations

> 아래는 요구사항이 아니며(AC 없음), Run phase의 설계 참고 사항이다.

- **`default_registry()` 신설 힌트(REQ-AI9-001)**: `claude_cli.rs:305-307`의 `claude_registry()`를 `default_registry()`로 리네임하고 codex를 추가 등록하는 방식을 상정한다. 또는 `claude_registry()`를 남기고 `default_registry()`를 별도 함수로 두는 방식도 허용된다 — 어느 쪽이든 `mod.rs:172`의 호출 지점이 새 함수를 가리키게 한다. 기존 `claude_registry()`를 호출하는 테스트(claude_cli.rs:551-572 `registry_registers_exactly_one_claude_adapter` 등)는 새 구조에 맞게 업데이트한다.
- **`ProviderRegistry::default_provider()` 강화 힌트(REQ-AI9-002)**: 현재 `default_provider()`는 단순 `first()`(provider.rs:111-113)를 반환한다. "installed && logged_in 필터"를 적용하려면 (a) `default_provider()` 자체를 강화하거나, (b) `first_available()` 신규 메서드를 두고 `route(None)`이 이를 호출하게 한다. (a)는 기존 테스트(provider.rs:193-207)에 영향을 주므로, (b)가 회귀 측면에서 안전하다.
- **`Capabilities` 분기 힌트**: codex `CodexProvider::capabilities()`는 `supports_streaming = false`(완성본 1회 도착이므로), `typical_latency_ms`는 실측 값(수 초)으로 둔다. 현재 `Capabilities`를 소비하는 분기는 없지만(모든 provider가 동일한 `relay_process` 경로를 탐), 향후 "비스트리밍 provider 전용 단일 emit 경로"를 도입할 때 분기 기준으로 쓰일 수 있다.
- **`build_codex_args` 인자 순서 검증 힌트(REQ-AI9-004)**: codex-cli 버전 업그레이드 시 플래그 호환성이 깨질 수 있다. 단위 테스트는 (a) 정확한 순서·개수·값의 스냅샷 어서션, (b) `--ignore-user-config`/`--sandbox read-only`/`--json` 등 격리·출력 플래그가 빠지지 않았는지 개별 어서션을 둔다.
- **codex 로그인 판정 고도화(REQ-AI9-016 확장)**: v1은 `auth.json` 존재만 본다. 향후 `auth.json`의 JSON 내용(예: 만료 시간)을 파싱해 토큰 만료를 선제 판정하려면 별도 REQ로 도입한다. 본 v1에서는 "파일 있음 = 로그인 됨" 휴리스틱만 유지한다.
- **`~/.codex/AGENTS.md` 오염 문서화(REQ-AI9-008 보조)**: `--ignore-user-config`는 `~/.codex/AGENTS.md` 자동 로딩을 차단하지만, 사용자 기기에 이 파일이 있으면 빈 cwd에서도 일부 컨텍스트 주입이 가능하다는 보고가 있다(실측: 32K 토큰 폭발 사례). 본 v1에서는 `--ignore-user-config` + 빈 cwd로 1차 방어하고, 추가 차단(예: 별도 환경변수)은 후속 SPEC에서 다룬다. Run phase는 `build_codex_args`에 `--ignore-user-config`가 반드시 포함되는지 단위 테스트로 검증한다(REQ-AI9-004).
- **~~`item.completed` JSON 형태 확정(REQ-AI9-009 구현 힌트)~~ [v0.0.3에서 해소됨]**: 0.0.1의 본 항목은 "정확한 JSON 경로(최상위 `type` vs `event.type`)를 Run phase에서 실제 출력을 붙잡아 확정한다"고 명시했으나, **그 확정 단계가 수행되지 않은 채** 래핑 형태를 가정한 구현이 머지되었다(결함 1의 근본 원인). 아래 "codex 0.144.1 실측 출력" 항목이 본 항목을 대체한다.
- **codex 0.144.1 실측 출력 원문(v0.0.3 신설, REQ-AI9-009/010 근거)**: `build_codex_args`가 산출하는 인자 벡터를 그대로 실행해 캡처한 stdout(JSONL). **`event` 래퍼가 없는 FLAT 형태**다:
  ```
  {"type":"thread.started","thread_id":"019f9446-..."}
  {"type":"turn.started"}
  {"type":"item.completed","item":{"id":"item_0","type":"agent_message","text":"Hi"}}
  {"type":"turn.completed","usage":{"input_tokens":14976,"cached_input_tokens":4480,"output_tokens":5,"reasoning_output_tokens":0}}
  ```
- **성공 시에도 codex stderr은 non-empty(v0.0.3 신설, 결함 1 진단 근거)**: codex는 정상 성공한 요청에서도 stderr에 다음 라인을 남긴다.
  ```
  ERROR codex_models_manager::cache: failed to load models cache: missing field `supports_reasoning_summaries`
  ```
  이 때문에 파서가 한 줄도 잡지 못하면 EOF 폴백 `decide_outcome(None, stderr, false, true)`이 비어있지 않은 stderr을 `classify_stderr`로 넘기고, 이 문자열은 login/network 마커에 걸리지 않아 `Other`로 분류된다. 즉 **파싱 실패가 진단에 유용한 `parse` 오류가 아니라 오도적인 `other`("잠시 문제가 있었어요") 오류로 퇴화**한다. 향후 파서 계약을 손볼 때 이 퇴화 경로를 기억할 것 — 증상만 보고는 파서 문제임을 알 수 없다.
- **fixture 날조 금지(v0.0.3 신설, 결함 1 재발 방지)**: 결함 1이 단위 테스트를 통과한 직접 원인은 `codex_cli.rs`의 `#[cfg(test)]` 헬퍼 `agent_message_line`(codex_cli.rs:489)이 **실제 출력이 아니라 SPEC 문서에 적힌 가정**으로 라인을 조립했기 때문이다. 외부 CLI의 출력 형태를 다루는 fixture는 반드시 실측 캡처에서 출발한다(AC-AI9-006).
- **provider 행 UI 구현 힌트(v0.0.3 신설, REQ-AI9-026~032)**: `deriveConnectionState`는 단일 "유효 provider"를 전제하므로 행 단위 상태에는 재사용하지 않는다. 행 상태는 `ProviderStatus` 하나만 받는 별도 순수 함수(예: `deriveProviderRowState(p): { label, selectable, reason }`)로 분리해 `SettingsModal.test.tsx`에서 렌더 없이 직접 단위 테스트한다. `deriveConnectionState` 자체는 섹션 수준 분기(loading / policy-locked / 온보딩 표시 여부)에 계속 쓰일 수 있으나, 그 값이 행 렌더 여부를 좌우해서는 안 된다(REQ-AI9-029).
- **파일 전환 정리 모듈 배치 힌트(v0.0.4 신설, REQ-AI9-033/034)**: `aiOffEffects.ts`의 `runAiOffCleanup` 본문(취소 → 고스트 clear → 레지스트리 clear)은 REQ-AI9-033이 요구하는 3동작과 **완전히 동일**하다. 따라서 본문을 복제하지 말고 공용 함수(예: `runAiArtifactCleanup()`)로 추출해 OFF 전이·파일 전환 두 트리거가 공유하는 것을 권장한다. 신규 구독 모듈은 `aiOffEffects.ts`의 형제로 두고(`initAiFileSwitchEffects()`), `AppLayout`의 기존 `useEffect(() => initAiToggleEffects(), [])` 옆에 동일한 형태로 1회 등록한다. `openFile`에 직접 호출을 심으면 워처·복원 경로가 `openFile`을 재사용할 때 정리가 예기치 않게 발동하고, `aiOffEffects.ts`가 문서화한 "재진입 없음" 속성도 깨진다(REQ-AI9-034).
- **파일 전환 감지 대상 힌트(REQ-AI9-033)**: `fileStore.currentFile`은 `openFile`의 모든 분기(html/raster/svg/too-large/text/binary)가 `setCurrentFile(path)`로 갱신하므로 파일 전환의 단일 신호로 충분하다(`useFileSystem.ts:150-180` 확인). `previewStatus`나 `editorStore.content`를 신호로 쓰면 같은 파일 재로딩·편집에도 오탐이 발생한다.
- **종결 phase 닫기 일괄 확장의 근거(v0.0.4 신설, REQ-AI9-037)**: `error` 하나만 닫을 수 있게 하면 "왜 이 카드는 닫히고 저 카드는 안 닫히나"라는 비일관성이 남는다. `empty`("이미 자연스러워서 바꿀 곳이 없어요")·`cancelled-by-new`("새 요청으로 취소되었어요")·`stale`("원문이 바뀌어 적용할 수 없어요")는 모두 **더 이상 진행할 동작이 없는 종결 상태**이므로 사용자가 확인 후 치우는 것이 유일한 다음 행동이다. 반면 `streaming`은 [✕ 취소], `intruded`는 [무시], `retry-exhausted`는 [⚡ 고급 모델로 다시 시도], `diagram-fallback`은 [✓ 목록으로]/[✕]로 이미 종료 경로를 갖고 있으므로 닫기를 추가하지 않는다(REQ-AI9-038 (b) 취지와 동일 — 중복 종료 컨트롤 금지).
- **terminal-empty 를 렌더 계층에서 파생하는 이유 — `aiStore` 리듀서 미변경(v0.0.5 신설, REQ-AI9-039~043)**: `reduceCompleteRequest`(`src/store/aiStore.ts:65-72`)의 `streamBuffer = finalText` 는 "완료 시 버퍼는 `ai://done` 의 `result` 라는 **권위 값**으로 확정된다"는 문서화된 계약이다(부록 A.1). 여기에 "빈 값이면 다른 상태로 바꾼다" 류의 분기를 넣으면 (a) 권위 값 계약이 깨지고, (b) 카드·고스트·향후 소비자 전부에 영향이 파급되며, (c) 카드 경로가 이미 자체 `reduceCard` 에서 `isEmptyOrIdentical` 로 분기하고 있어 판정이 **두 곳으로 분산**된다. 따라서 본 개정은 스토어를 건드리지 않고, 고스트가 이미 보유한 두 신호(`text` 와 `status`)의 조합 — `status === 'done' && text.trim() === ''` — 으로 terminal-empty 를 **렌더 계층에서 파생**한다. 이 조합은 `ghostDecorations`(`ai-ghost-text.ts:257-269`)와 `GhostControlsWidget` 두 지점에서만 관측되므로 변경 표면이 파일 1개로 국소화된다.
- **카드 헬퍼 공유 여부(v0.0.5 신설, REQ-AI9-040 구현 힌트)**: 판정 의미론은 `isEmptyOrIdentical`(`ai-suggestion-card.ts:72-75`)의 빈/공백-only 부분과 동일해야 하지만, 그 헬퍼는 `(finalText, original)` 2인자이고 고스트에는 `original` 이 없다. 따라서 (a) `isEmptyOrIdentical(text, '')` 로 그대로 재사용하거나(빈 원문이면 `trimmed === ''` 판정과 등가), (b) 두 곳이 공유하는 1인자 헬퍼(예: `isEffectivelyEmpty(text: string): boolean`)를 추출하고 `isEmptyOrIdentical` 이 이를 호출하도록 얇게 리팩터하는 두 선택지가 있다. 어느 쪽이든 **판정 문자열 규칙을 새로 작성하는 것은 금지**한다(REQ-AI9-040). (b)를 택할 경우 `isEmptyOrIdentical` 의 기존 시그니처·동작은 무변경이어야 하며 카드 테스트가 무수정 통과해야 한다.
- **고급 티어 사고 예산을 "미설정"으로 두는 이유(v0.0.6 신설, REQ-AI9-046/047)**: 고급 티어에 `MAX_THINKING_TOKENS` 를 어떤 숫자로 넣든(4000, 8000, …) 그 숫자의 출처를 문서화할 수 없다 — 우리가 측정한 값도, claude CLI 가 문서화한 권장값도 아니다. 무출처 상수는 (a) 사후 리뷰에서 아무도 정당화하지 못하고, (b) CLI 가 기본값을 조정해도 따라가지 못하며, (c) "왜 4000인가"를 묻는 다음 사람에게 답이 없다. 반면 **환경변수를 설정하지 않으면 claude CLI 자체 기본값이 적용된다** — 이는 자기문서적("우리는 CLI 판단에 위임한다")이고 버전업에 자동 추종한다. 기본 티어의 `0` 은 반대로 출처가 분명하다: "이 앱의 AI 기능은 전부 순수 텍스트 변환이라 추론이 불필요하고, 0 은 지연을 최소화한다"는 기존 설계 의도(`claude_cli.rs:71` 주석 계보)의 명시적 표현이다. 즉 `0` 은 의도된 값이고, 고급 티어의 부재는 의도된 위임이다. 만약 Run phase 에서 "미설정이 곧 0 과 같다"(CLI 기본값이 0)거나 CLI 가 값 없이는 동작하지 않는 등 **이 접근이 성립하지 않는 구체적 근거**를 발견하면, 숫자를 임의로 채워 넣지 말고 그 근거를 보고해 SPEC 개정으로 처리한다.
- **컨텍스트 문자 상한은 본 개정 범위 밖(v0.0.6 신설, 연기 항목)**: `prompt.rs:10-14` 의 `INLINE_SIDE_MAX = 1000`, `SECTION_TAIL_MAX = 1500`, `CONTINUE_HEAD_MAX = 1500` 은 고급 티어에서도 **변경하지 않는다**(사용자 확정). 이 값들은 **모델 컨텍스트 한계가 아니라 지연·초점 설계값**이다 — haiku/sonnet/gpt-5.5 세 모델 모두 컨텍스트 창이 1500자를 압도적으로 상회하므로 "고급 모델이니 더 많이 넣자"는 추론은 근거가 없고, 프롬프트가 길어질수록 지연만 늘고 모델의 초점은 흐려진다. 재검토가 필요해지면 **`truncated` 플래그가 실제 사용에서 얼마나 자주 뜨는지 계측하는 것에서 시작**해야 한다(현재 이 수치를 아무도 모른다). 향후 구현자가 "친절하게" 이 상수를 올리는 것을 막기 위해 본 항목과 Exclusions 에 명시적으로 못박는다. `prompt.rs` 는 본 개정에서도 변경 라인 0건이다(REQ-AI9-024 가드 유지).
- **`AiModel` 명명 결정 — (a) 이름 유지 + 관례상 티어 취급(v0.0.6 신설, 재론 금지)**: `AiModel { Haiku, Sonnet }` 은 Anthropic 모델명에서 왔으나 실질은 provider 무관 티어(기본/고급)다. 두 선택지를 저울질한 결과 **(a) enum 이름을 유지하고 매핑표로 티어 의미를 문서화**를 택한다. 근거: (1) **폭발 반경** — 이름에 결합된 지점이 `AiModel::from_opt`(문자열 `"sonnet"` 매칭), `AiModel::as_arg`(claude 에서는 **이름이 곧 정답**: 반환값이 실제 CLI 인자 `haiku`/`sonnet` 이다), `codex_model_arg`/`codex_reasoning_effort`, IPC 문자열 도메인 `'haiku'|'sonnet'`, 프론트 `resolveModel` 2곳, 그리고 이 문자열을 단언하는 기존 테스트들이다. (2) **이름만 바꿔서는 문제가 해결되지 않는다** — 진짜 "claude 냄새"는 enum variant 가 아니라 **IPC 문자열 값**(`model?: 'haiku'|'sonnet'`)이고, 이는 프론트-백엔드 하위호환 표면이다. enum 만 `Basic`/`Advanced` 로 바꾸면 `from_opt` 안에 여전히 `"sonnet"` 리터럴이 남아 **이름과 값이 어긋나는 더 나쁜 상태**가 된다. (3) IPC 문자열까지 함께 바꾸면 하위호환이 깨지는데(구버전 프론트·영속화된 값·`aiProviderId.test.ts` 계보 테스트) **사용자에게 관측되는 개선은 0** 이다 — 순수 명명 리팩터로 회귀 위험만 산다. (4) REQ-AI9-019 가 이미 "이름 유지"를 계약으로 고정했고 v0.0.6은 이를 **번복하지 않는다**. 대신 티어 의미를 "Model Mapping" 표 2개(모델 값 / 추론 강도)로 명시해 이름-의미 간극을 문서로 메운다. 향후 3번째 provider 도입 등으로 매핑이 3곳 이상으로 흩어지면, 그때 **IPC 문자열까지 포함한 일괄 개명**을 별도 SPEC 으로 다룬다(부분 개명 금지).
- **라벨은 백엔드가 공급한다 — 매핑 소유자가 표시 문자열도 소유(v0.0.6 작성, v0.0.7 채택으로 갱신, REQ-AI9-048/049/051/052 구현 힌트)**: 라벨에 실제 모델명(`sonnet`, `gpt-5.5`)을 넣으려면 그 문자열이 어딘가에 **복제**되어야 하고, 매핑이 개정되는 순간(예: codex 가 다른 모델로 이동) 라벨은 **조용히 거짓말을 시작**한다 — 결함 5가 정확히 그 형태다(라벨이 `sonnet` 이라 주장하는데 codex 는 `gpt-5.5` 를 쓴다). 이 위험은 복제본이 프론트에 있든 Rust 에 있든 동일하다. **따라서 매핑의 소유자(Rust)가 표시 문자열도 소유한다**: `ProviderStatus.advanced_model_label` 을 실제 인자 조립과 **같은 함수**(`codex_model_arg`/`codex_reasoning_effort`/`AiModel::as_arg`)에서 파생해 IPC 로 내려주고, 프론트는 그것을 **그대로 렌더**한다. 이 구조에서는 매핑을 바꾸면 라벨이 **자동으로 따라오므로 어긋날 수가 없다** — 규율이 아니라 구조로 보장된다는 점이 이 설계의 전부다. 대가는 IPC 표면 1필드 확대(Rust `ProviderStatus` + TS `AiProviderStatus` 동반 수정)이며, v0.0.6은 이 비용을 이유로 연기했으나 **v0.0.7에서 사용자 확정으로 채택**되었다. ~~"모델명 리터럴 없이 `{표시명} 고급 티어` 로만 구성"(v0.0.6)~~ 은 **필드가 비었을 때의 폴백**으로 강등되었다(REQ-AI9-053) — 3번째 provider 나 구버전 백엔드에서 여전히 안전하게 렌더되어야 하기 때문이다. 알 수 없는 추론 강도 키를 원문 그대로 통과시키는 `codex_effort_display` 의 규칙(REQ-AI9-052)도 같은 정신이다: **모르면 침묵하거나 날것을 보여줄지언정, 틀린 것을 자신 있게 말하지 않는다.**
- **모델 매핑 중앙화(REQ-AI9-018 구현 힌트)**: `build_codex_args` 내부에서 `match model { Haiku|Sonnet => "gpt-5.5" }`로 직접 처리하는 것이 가장 단순하다. `AiProvider` trait에 `fn model_arg(&self, AiModel) -> &str`를 추가하는 것은 확장성은 좋으나 기존 trait 호출부(claude_cli.rs:291 `build_claude_args(request.model, ...)`)에 영향을 주므로 v1에서는 과잉 설계다. 매핑 로직이 3곳 이상으로 퍼지면 그때 trait 메서드로 통합한다.

## Delta (Brownfield Changes)

| Delta | 파일 | 변경 내용 |
|-------|------|-----------|
| [NEW] | `src-tauri/src/ai/codex_cli.rs` | 신규 파일. `CodexProvider` 구조체 + `build_codex_args`(순수, REQ-AI9-004) + `spawn_codex`(REQ-AI9-007) + `CodexProvider::capabilities()`(supports_streaming=false, typical_latency_ms=실측) + `CodexProvider::detect()` 위임(detect.rs). `claude_cli.rs` 구조를 차용, 예상 ~150줄. |
| [MODIFY] | `src-tauri/src/ai/stream.rs` | `parse_codex_agent_message`(REQ-AI9-009) + `parse_codex_turn_completed`(REQ-AI9-010) 순수 함수 추가. 기존 `parse_text_delta`/`parse_final_result`/`classify_stderr`은 무변경(REQ-AI9-023). `#[cfg(test)]`에 codex JSONL fixture 단위 테스트 추가. |
| [MODIFY] | `src-tauri/src/ai/detect.rs` | `resolve_codex_binary`(REQ-AI9-015) + `codex_binary`(캐시 래퍼, `claude_binary` detect.rs:251-258과 대칭) + `is_codex_logged_in`(REQ-AI9-016) + `detect_codex`(REQ-AI9-017) 추가. 기존 claude 함수들은 무변경. `#[cfg(test)]`에 codex 경로/로그인 판정 단위 테스트 추가. |
| [MODIFY] | `src-tauri/src/ai/claude_cli.rs` 또는 `provider.rs` | 기존 `claude_registry()`를 `default_registry()`로 리네임하거나(또는 신규 `default_registry()` 추가) `ProviderRegistry::first_available()`(REQ-AI9-002) 신설. 어느 쪽이든 `mod.rs:172`의 호출 지점이 새 함수를 가리키게 업데이트. 기존 `claude_registry()` 단위 테스트(claude_cli.rs:551-572)는 새 구조에 맞게 이동/확장(claude 1개 + codex 1개 등록 어서션으로 교체). |
| [MODIFY] | `src-tauri/src/ai/claude_cli.rs` (`#[cfg(test)]`) | **REQ-AI9-022 회귀 스냅샷 추가** — `build_claude_args`의 `(AiModel, &str, &str)` 입력별 산출 `Vec<String>`을 하드코딩 어서션으로 고정(프로덕션 코드 `build_claude_args` 자체는 무변경). 본 행은 plan.md M1.1과 일치하며, codex 통합 코드가 의도치 않게 claude 인자 시퀀스를 건드린 경우 즉시 포착. |
| [MODIFY] | `src-tauri/src/ai/mod.rs` | (a) `ai_request`(mod.rs:122)가 `default_registry()`를 쓰도록 호출 지점 변경(이미 `route(args.provider_id.as_deref())`는 호출 중). (b) codex 릴레이 진입 시 `parse_codex_agent_message`/`parse_codex_turn_completed`를 쓰는 경로 분기 추가 — 단, 분기는 provider id 또는 `Capabilities::supports_streaming`으로 판단. `claude` 경로는 기존 `parse_text_delta`/`parse_final_result` 그대로(REQ-AI9-023 회귀 가드). |
| [MODIFY] | `src-tauri/src/ai/codex_cli.rs` (`#[cfg(test)]`) | `build_codex_args` 인자 순서/개수/값 스냅샷, 모델 매핑(Haiku/Sonnet → gpt-5.5), stdin null/절대경로/cwd 설정 단위 테스트 추가. |
| [MODIFY] | `src-tauri/src/ai/stream.rs` (`#[cfg(test)]`) | codex JSONL fixture(`thread.started`/`turn.started`/`item.completed(agent_message)`/`turn.completed`/비정형 라인)로 `parse_codex_agent_message`/`parse_codex_turn_completed` 단위 테스트 추가. 기존 claude 파서 테스트(stream.rs:128-258) 무변경(REQ-AI9-023). |
| [MODIFY] | `src-tauri/src/ai/detect.rs` (`#[cfg(test)]`) | `resolve_codex_binary` 후보 순서·표준 위치 포함 어서션, `is_codex_logged_in` true/false 케이스, `detect_codex` 미설치 환경 panic 없음 어서션 추가. |
| [NEW] | `src-tauri/src/ai/claude_cli.rs` (`#[cfg(test)]`) 또는 `provider.rs` (`#[cfg(test)]`) | REQ-AI9-001 registry 2개 등록(claude+codex) 어서션 + REQ-AI9-002 자동 감지 우선순위(claude 설치 시 claude 기본, claude 미설치 시 codex 폴백) 회귀 테스트. |
| [MODIFY] | `src-tauri/src/ai/codex_cli.rs` (`#[cfg(test)]`) *(v0.0.3)* | **결함 1 재발 방지** — fixture 헬퍼 `agent_message_line`(codex_cli.rs:489)이 날조한 래핑 형태를 **실측 FLAT 캡처**로 교체. 래핑 fallback 검증용 헬퍼는 별도 이름으로 분리해 "primary vs fallback"을 테스트 이름에서 구분(AC-AI9-006). |
| [MODIFY] | `src/components/settings/SettingsModal.tsx` *(v0.0.3)* | **결함 2 수정** — (a) Claude 전용 하드코딩 상태 블록(~180/196/209행의 리터럴 "Claude Code" + 배지·버전·온보딩 문구) 제거, (b) `AiProviderSelect` 드롭다운(~358행~) 제거 후 `ai_detect_providers` 결과 배열 순회 기반 대등 라디오 행 목록으로 대체(REQ-AI9-026/029/030), (c) 행 상태 파생 순수 함수 신설(REQ-AI9-027), (d) 미사용 행 `disabled` + 인라인 사유(REQ-AI9-028), (e) 정책 잠금 🔒 유지(REQ-AI9-031), (f) 미로그인 행에서 `onStartOnboarding` 도달 경로 보존(REQ-AI9-032). `aiSelectedProvider` 영속화 키·`providerId` IPC 계약은 무변경. |
| [MODIFY] | `src/lib/ai/aiOffEffects.ts` *(v0.0.4)* | **결함 3a** — `runAiOffCleanup` 본문(취소 → 고스트 clear → 레지스트리 clear)을 공용 함수(예: `runAiArtifactCleanup()`)로 **추출**하고 `runAiOffCleanup`은 이를 호출하는 얇은 래퍼로 남긴다(중복 구현 금지, Design Notes). 기존 export 시그니처(`runAiOffCleanup`/`initAiToggleEffects`)와 OFF 전이 동작은 무변경(SPEC-AI-005 REQ-AI5-011/012 회귀 금지). |
| [NEW] | `src/lib/ai/aiFileSwitchEffects.ts` *(v0.0.4)* | **결함 3a** — `initAiFileSwitchEffects(): () => void` 신규 모듈. `useFileStore.subscribe`로 `currentFile` 전이를 관찰해 `runAiArtifactCleanup()`을 1회 호출(REQ-AI9-033/034). `aiOffEffects.ts`의 구조를 그대로 차용하며 콜백 내부에서 `useFileStore`를 재사용하지 않는다(재진입 없음). |
| [MODIFY] | `src/components/layout/AppLayout.tsx` *(v0.0.4)* | **결함 3a** — 기존 `useEffect(() => initAiToggleEffects(), [])`(AppLayout.tsx:84 인근) 옆에 `useEffect(() => initAiFileSwitchEffects(), [])`를 **마운트 1회** 등록(REQ-AI9-034). 그 외 로직 무변경. |
| [MODIFY] | `src/components/editor/extensions/ai-suggestion-card.ts` *(v0.0.4)* | **결함 3b** — (a) `error` phase 렌더(329-353행)의 3개 kind 분기 전부에 `닫기` 컨트롤 추가(REQ-AI9-036), (b) `empty`(321-327)·`cancelled-by-new`(372-379)·`stale`(381-387) 분기에도 동일 컨트롤 추가(REQ-AI9-037), (c) 닫기 핸들러는 해당 카드를 레지스트리에서 제거하는 부수효과 **1가지만** 수행하며 문서 텍스트를 건드리지 않는다(REQ-AI9-038). `streaming`/`intruded`/`retry-exhausted`/`diagram-fallback` 분기는 무변경. |
| [MODIFY] | `src/test/aiOffEffects.test.ts` / `src/test/aiFileSwitchEffects.test.ts` *(v0.0.4)* | AC-AI9-020·021 테스트 — 파일 전환 시 취소·고스트·레지스트리 3동작 수행, 동일 파일 재설정 시 미발동, 정리가 문서 텍스트를 변경하지 않음(무손상 불변), OFF 전이 기존 동작 회귀 없음. |
| [MODIFY] | `src/test/aiSuggestionCard.test.ts` *(v0.0.4)* | AC-AI9-022·023·024 테스트 — `error` 3종 kind 전부에 닫기 컨트롤 존재, 종결 phase(`empty`/`cancelled-by-new`/`stale`) 닫기 확장, `streaming`에는 닫기 부재, 닫기 후 레지스트리에서 제거·문서 텍스트 무변경, **파일 A 오류 카드가 파일 B 열기 후 소멸(회귀 AC)**. |
| [MODIFY] | `src/components/editor/extensions/ai-ghost-text.ts` *(v0.0.5)* | **결함 4 수정(단일 변경 지점)** — (a) `ghostDecorations`(257-269행)가 `status === 'done' && text.trim() === ''` 조합을 terminal-empty 로 파생해 `GhostPlaceholderWidget` 대신 안내 위젯(`ℹ 더 쓸 내용을 찾지 못했어요`)을 렌더(REQ-AI9-039/041), (b) `GhostControlsWidget`(363-389행)이 terminal-empty 에서 [✓ 넣기]/[✕ 지우기]/↻ 대신 [✕ 닫기] **1개만** 렌더(REQ-AI9-039/044), (c) 닫기 핸들러는 기존 `dismissGhostCommand` 를 재사용해 문서 텍스트를 건드리지 않는다(REQ-AI9-042). 스트리밍 중(`status === 'streaming'`) 빈 텍스트 경로와 비어있지 않은 done 경로는 **무변경**. |
| [NOT MODIFIED] | `src/store/aiStore.ts` *(v0.0.5, 의도적 미변경)* | `reduceCompleteRequest` 의 `streamBuffer = finalText`(권위 값 계약)를 **건드리지 않는다**. 빈 결과 판정은 렌더 계층에서 파생한다(근거는 Design Notes "terminal-empty 를 렌더 계층에서 파생하는 이유"). 본 행은 "변경 없음"을 명시적 계약으로 고정하기 위한 항목이다. |
| [NOT MODIFIED] | `src-tauri/src/ai/codex_cli.rs` / `stream.rs` *(v0.0.5, 의도적 미변경)* | 빈 `result` 를 담은 `ai://done` emit은 적법하며 억제·오류 전환하지 않는다(REQ-AI9-043). codex `relay_codex_process` 의 `last_message.clone().unwrap_or_default()` 와 claude `parse_final_result` 는 무변경. |
| [MODIFY] | `src/test/aiGhostEmptyDone.repro.test.ts` → 정식 회귀 테스트 *(v0.0.5)* | 현행 파일은 **임시 재현 스캐폴드**다(`console.log` 포함, 단언 1개). M9에서 저장소 명명 관례에 맞는 정식 회귀 테스트로 **전환하거나 삭제 후 재작성**한다 — `console.log` 제거, AC-AI9-026의 3단언(플레이스홀더 부재·넣기 부재·닫기 존재) 전체로 강화. 현행 형태로 잔존시키지 않는다. |
| [MODIFY] | `src-tauri/src/ai/codex_cli.rs` *(v0.0.6)* | **결함 5 — codex 티어 파생** — `codex_reasoning_effort(model: AiModel) -> &'static str` 순수 함수 신설(`Haiku→"medium"`, `Sonnet→"high"`, `codex_model_arg` 의 형제 위치), `build_codex_args` 의 하드코딩 `"model_reasoning_effort=\"medium\""` 을 `format!` 파생으로 교체(REQ-AI9-045). 인자 원소 개수(15)·순서·그 외 값은 무변경. `codex_model_arg` 는 무변경(양 티어 `gpt-5.5`). |
| [MODIFY] | `src-tauri/src/ai/claude_cli.rs` *(v0.0.6)* | **결함 5 — claude 티어 파생** — `claude_thinking_env(model: AiModel) -> Option<(&'static str, &'static str)>` 순수 함수 신설(`Haiku→Some(("MAX_THINKING_TOKENS","0"))`, `Sonnet→None`), `spawn_claude`(170-185행)가 무조건 `.env("MAX_THINKING_TOKENS","0")` 하던 것을 `Some` 일 때만 설정하도록 변경 + 모델을 받도록 시그니처 확장(`spawn_claude(args, cwd, model)`), 호출부 `ClaudeProvider::spawn`(292행) 동반 수정(REQ-AI9-046/047). **`build_claude_args` 는 무변경**(REQ-AI9-022 바이트 동등 가드 유지) — `--model` 값은 기존 `AiModel::as_arg()` 가 이미 티어별로 분기한다. 71행 독스트링의 "`MAX_THINKING_TOKENS=0` env는 `spawn_claude`에서 설정" 문구를 티어 조건부로 갱신. |
| [MODIFY] | `src-tauri/src/ai/codex_cli.rs` / `claude_cli.rs` (`#[cfg(test)]`) *(v0.0.6)* | AC-AI9-028·029 테스트 — codex 기본/고급 인자 벡터 **원소별 비교로 정확히 1개 원소만 상이** 단언 + 기존 스냅샷 테스트(`build_codex_args_haiku_snapshot_exact_14_elements`·`build_codex_args_sonnet_also_maps_to_gpt55`·`build_codex_args_isolation_flags_never_missing`)를 **의도적으로 갱신**(격리 플래그 단언은 완화 금지). claude 측은 `claude_thinking_env` 2케이스 + 기존 `build_claude_args_*` 스냅샷 3종 **무수정 통과** 확인. |
| [MODIFY] | `src-tauri/src/ai/provider.rs` *(v0.0.7)* | **결함 5 — 백엔드 공급 라벨** — `ProviderStatus`(44-55행)에 `#[serde(skip_serializing_if = "Option::is_none")] pub advanced_model_label: Option<String>` **선택 필드 1개만** 추가(REQ-AI9-051). `AiModel` enum variant 이름·`as_arg`·`from_opt`·`Capabilities`·`AiProvider` trait 시그니처는 **무변경**(REQ-AI9-019/050 (d)). *(v0.0.6이 본 파일을 "diff 0줄"로 단언했던 것은 v0.0.7에서 이 범위로 정정된다.)* |
| [MODIFY] | `src-tauri/src/ai/claude_cli.rs` / `codex_cli.rs` *(v0.0.7)* | **결함 5 — 라벨 파생(단일 소스)** — 각 파일에 고급 티어 라벨 조립 함수를 1개씩 두고, `AiProvider::detect()` 구현(`ClaudeProvider::detect` 286-288행, `CodexProvider::detect`)이 `detect::detect_*()` 결과에 `advanced_model_label` 을 채워 반환한다(REQ-AI9-051). claude 라벨 = `AiModel::as_arg(AiModel::Sonnet)`, codex 라벨 = `format!("{} · {}", codex_model_arg(Sonnet), codex_effort_display(codex_reasoning_effort(Sonnet)))` + `codex_effort_display`(알 수 없는 키는 원문 통과) 신설(REQ-AI9-052). **모델명·추론 강도 문자열 리터럴을 라벨 조립부에 적지 않는다**(REQ-AI9-049 (b)). |
| [MODIFY] | `src-tauri/src/ai/detect.rs` *(v0.0.7)* | `detect_claude`(282-287행)·`detect_codex` 의 `ProviderStatus` 생성 지점에 `advanced_model_label: None` 추가(컴파일 요건). **매핑 지식을 `detect.rs` 로 들이지 않는다** — 값 채움은 어댑터의 `detect()` 책임이다(REQ-AI9-051). 그 외 감지 로직 무변경. |
| [MODIFY] | `src/lib/tauri/ipc.ts` *(v0.0.7)* | `AiProviderStatus`(259-264행)에 `advancedModelLabel?: string` **선택 필드 1개만** 추가(REQ-AI9-051). `AiRequestArgs`·`AiPolicyStatus`·이벤트 payload 타입은 **무변경**(REQ-AI9-021/050 (d)). *(v0.0.6이 본 파일을 "diff 0줄"로 단언했던 것은 v0.0.7에서 이 범위로 정정된다.)* |
| [MODIFY] | `src/components/settings/SettingsModal.tsx` *(v0.0.6, v0.0.7 개정)* | **결함 5 — 라벨 provider 인지화** — `AdvancedModelToggle`(269-293행)의 라벨 `고급 모델 사용 (sonnet — 더 정확, 더 느림)` 과 `aria-label="고급 모델 사용 (sonnet)"` 에서 `"sonnet"` 리터럴 제거. *(v0.0.7)* 유효 provider(`uiStore.aiSelectedProvider`, `'auto'` 면 첫 selectable)의 **`advancedModelLabel` 을 그대로** 감싸 렌더하고(REQ-AI9-048), 필드가 부재·빈 값이면 `providerDisplayName` 기반 폴백, 유효 provider 부재면 중립 문구로 폴백한다(REQ-AI9-053). 모델명 리터럴 테이블 신설 금지. `resolveModel`(46-48행)·`deriveProviderRowState`·`ProviderRow`·행 목록은 **무변경**. |
| [MODIFY] | `src/test/SettingsModal.test.tsx` *(v0.0.6, v0.0.7 개정)* | AC-AI9-030 테스트 — mock 이 내려준 `advancedModelLabel`(`sonnet` / `gpt-5.5 · 높은 추론`)이 라벨·`aria-label` 에 **그대로 포함**됨, 프론트 소스 유래 리터럴 `sonnet` 부재, `'auto'` 유효 provider 판정, **폴백 3경로**(필드 부재 / 빈·공백 문자열 / 유효 provider 부재), `undefined` 문자열 미노출, 정책 잠금 시 기존 `disabled`+🔒 관례 무변경. 기존 AC-AI9-016~019 테스트는 무수정 통과. |
| [MODIFY] | `src-tauri/src/ai/claude_cli.rs` / `codex_cli.rs` (`#[cfg(test)]`) *(v0.0.7)* | AC-AI9-031 테스트 — 두 라벨 함수의 산출값이 `AiModel::as_arg(Sonnet)` / `codex_model_arg(Sonnet)` 과 `codex_reasoning_effort(Sonnet)` 로부터 파생됨을 **중앙 함수 반환값과 대조해** 단언(하드코딩 기대 문자열 단독 비교 금지), `codex_effort_display` 의 알 수 없는 키 원문 통과, `detect()` 가 `advanced_model_label` 을 `Some(non-empty)` 로 채움. 라벨 조립부에 `"sonnet"`/`"gpt-5.5"`/`"high"` 리터럴 부재를 소스 검사로 확인. |
| [NOT MODIFIED] | `src-tauri/src/ai/prompt.rs` *(v0.0.6, 의도적 미변경)* | `INLINE_SIDE_MAX`/`SECTION_TAIL_MAX`/`CONTINUE_HEAD_MAX`(10-14행)를 고급 티어라고 해서 올리지 않는다 — 지연·초점 설계값이지 모델 컨텍스트 한계가 아니다(Design Notes, Exclusions). 변경 라인 0건(REQ-AI9-024 가드 연장). |
| [NOT MODIFIED] | `src-tauri/src/ai/provider.rs` *(v0.0.6, 의도적 미변경)* | `AiModel` variant 이름(`Haiku`/`Sonnet`)·`as_arg`·`from_opt` 무변경(REQ-AI9-019 계약 유지, Design Notes "AiModel 명명 결정"). `src/lib/tauri/ipc.ts` 의 `model?: 'haiku'\|'sonnet'` 도 무변경. |
| [MODIFY] | `src/test/SettingsModal.test.tsx` *(v0.0.3)* | AC-AI9-016~019 테스트 추가(행 렌더 대칭성·registry 순서 구동·3번째 provider 무수정 렌더·행별 상태 파생·미사용 행 선택 차단+인라인 사유·드롭다운 부재·정책 잠금·온보딩 진입점). 기존 드롭다운(`aria-label="AI 엔진 선택"`) 대상 테스트는 행 목록 대상으로 교체. |

> 핵심 불변(단일 소스 원칙): 기존 `claude` 관련 코드(`build_claude_args`, `parse_text_delta`, `parse_final_result`, `classify_stderr`, `ClaudeProvider`, `spawn_claude`, prompt.rs 전체)는 **직접 수정하지 않는다**(REQ-AI9-022, REQ-AI9-023, REQ-AI9-024). codex는 신규 파일·신규 함수로 분리되며, 공유 지점은 `ProviderRegistry`와 `route()` 호출뿐이다.

## Acceptance Criteria

> 백엔드 단위는 Rust `#[cfg(test)]`로 검증(순수 함수 중심), 프론트 단위는 Vitest + Testing Library로 검증한다. 통합 검증(codex 실제 호출)은 로컬 수동 테스트로 별도 수행. 각 AC의 Given-When-Then 상세는 sibling `acceptance.md`(AC-AI9-001~027)에 전개되어 있다. 개발 모드는 TDD이므로 각 AC는 **실패하는 테스트를 먼저** 쓸 수 있도록 관측 가능한 단언으로 기술한다.

| AC ID | Requirement | Summary |
|-------|-------------|---------|
| AC-AI9-001 | REQ-AI9-001, 002, 003 | `default_registry()`가 `claude`+`codex` 정확히 2개 등록; 자동 감지가 "설치+로그인" 첫 provider 선택(claude 설치 시 claude, codex만 사용 가능 시 codex, 둘 다 불가 시 None) |
| AC-AI9-002 | REQ-AI9-001, 025 | registry.ids() == ["claude", "codex"]; `providerId` 명시 시 해당 provider 강제 라우팅, 미명시 시 우선순위 적용; codex가 claude 설치 환경에서 기본이 되지 않음 |
| AC-AI9-003 | REQ-AI9-004, 018 | `build_codex_args(Haiku, "combine")` 산출 `Vec<String>`이 플래그 전체(`exec`/`-C`/`--ignore-user-config`/`--skip-git-repo-check`/`--ephemeral`/`--sandbox read-only`/`--model gpt-5.5`/`-c model_reasoning_effort="medium"`/`--json`/결합 프롬프트)를 정확한 순서로 포함 |
| AC-AI9-004 | REQ-AI9-005 | system+user 결합이 `format!("{}\n\n{}", s, u)`로 단일 인자 생성; codex 산출 인자의 마지막 원소가 결합 문자열과 바이트 동일 |
| AC-AI9-005 | REQ-AI9-006, 007, 008 | `spawn_codex`가 `.stdin(Stdio::null())` + `.stdout(piped())` + `.stderr(piped())` + `no_window` + 빈 scratch cwd + 절대경로 `codex_binary()` 사용; `CODEX_HOME` 환경변수 설정 코드 부재 |
| AC-AI9-006 *(v0.0.4 개정)* | REQ-AI9-009, 010 | **fixture는 codex-cli 0.144.1 실측 캡처(FLAT)에서 출발**한다 — `parse_codex_agent_message`가 FLAT `{"type":"item.completed","item":{"type":"agent_message","text":...}}`(PRIMARY)와 래핑 `{"type":"event","event":{...}}`(FALLBACK) **둘 다**에서 `item.text` 추출(`Some`), 그 외 이벤트/비정형 라인은 `None`; `parse_codex_turn_completed`가 FLAT `{"type":"turn.completed",...}`(PRIMARY)와 래핑(FALLBACK) 둘 다에서 `true`, 그 외 `false`. **손으로 날조한 형태를 유일 fixture로 쓰는 것을 금지**한다(결함 1 재발 방지 — `codex_cli.rs:489` `agent_message_line` 헬퍼가 실측 대신 SPEC 문서의 가정으로 라인을 조립해 결함을 통과시켰다). PRIMARY/FALLBACK 테스트는 이름으로 구분한다. |
| AC-AI9-007 | REQ-AI9-011, 012 | codex 릴레이가 `agent_message` 수신 시 `ai://chunk`를 정확히 1회 emit(토큰 분할 금지); `turn.completed` 수신 시 `ai://done`({result=마지막 agent_message, truncated=전달값}) emit |
| AC-AI9-008 | REQ-AI9-013, 014 | codex stdout이 비었거나 비정형 라인만 있는 채 EOF 시 기존 `decide_outcome` 경로로 폴백(`parse`/`other` kind `ai://error`); codex stderr은 기존 `classify_stderr`로 분류(login/network/other) — codex 전용 분류 코드 부재 |
| AC-AI9-009 | REQ-AI9-015 | `resolve_codex_binary`가 PATH 후보 + 플랫폼 표준 위치(macOS/Linux/Windows) 후보를 생성하고 첫 존재 경로 반환; 미설치 환경에서 `None`(panic 없음) |
| AC-AI9-010 | REQ-AI9-016, 017 | `is_codex_logged_in(home)`이 `~/.codex/auth.json` 존재 시 `true`, 부재 시 `false`; `detect_codex()`가 `id="codex"` + installed/logged_in 상태를 panic 없이 반환 |
| AC-AI9-011 *(v0.0.4 개정)* | REQ-AI9-018, 019, 020 | codex `--model` 매핑이 Haiku/Sonnet 모두 `gpt-5.5`; `AiModel` enum variant 무변경; **프론트 변경 범위가 REQ-AI9-020의 허용 표면으로 한정** — `src/components/` 변경이 `settings/SettingsModal.tsx`·`layout/AppLayout.tsx`·`editor/extensions/ai-suggestion-card.ts` 3개 파일에 머무르고, provider별 개별 설정 화면·상시 "codex 사용 중" 인디케이터·설정 다이얼로그 외부 provider 선택 컨트롤 신규 컴포넌트가 부재 |
| AC-AI9-012 | REQ-AI9-021 | IPC 계약 변경 없음 — `AiRequestArgs` 신규 필드 부재, `provider_id`만 재사용; `ai://chunk`/`done`/`error` payload 스키마 무변경 |
| AC-AI9-013 | REQ-AI9-022, 023 | 회귀 가드 통과 — `build_claude_args` 동일 입력에 동일 산출(바이트 동등 스냅샷); `parse_text_delta`/`parse_final_result` 동일 입력에 동일 `Option<String>`(단위 테스트 전수 통과) |
| AC-AI9-014 | REQ-AI9-024 | `prompt.rs` 조립 회귀 가드 통과 — 비-diagram 5기능(polish/outline/table/shorten/custom) + diagram + section + continue 전 분기가 동일 입력에 동일 `(system_prompt, user_prompt)` 산출(바이트 동등 스냅샷, SPEC-AI-008 AC-AI-008-014와 동일 계약) |
| AC-AI9-015 | REQ-AI9-004, 008 | **수동 통합** — `~/.codex/AGENTS.md`(또는 글로벌 스킬)가 존재하는 사용자 환경에서 codex 요청 실행 시 AI 응답이 해당 파일 내용에 영향받지 않음(빈 환경 대비 응답 범위 동일, `usage.input_tokens`가 폭발하지 않음). `--ignore-user-config` + 빈 cwd가 1차 방어선으로 기능함을 확인. 완전 자동화가 어려워 수동 테스트로 표기(M7 단계). |
| AC-AI9-016 *(v0.0.4 신설)* | REQ-AI9-026, 029 | `ai_detect_providers`가 2개 provider를 반환하면 `AI 도구` 섹션에 **형태가 동일한 2행**이 렌더된다(각 행 = radio + 표시명 + 상태 배지 + 버전, 동일 `name` 그룹). 행 순서는 IPC 반환 배열 순서와 동일하며, mock이 3번째 provider를 반환하면 `SettingsModal.tsx` 수정 없이 3행이 렌더된다. `providers.find(p => p.id === 'claude')` 류 하드코딩 조회로 행을 구성하지 않는다 |
| AC-AI9-017 *(v0.0.4 신설)* | REQ-AI9-027 | 행 상태 파생이 **렌더 없이 호출 가능한 순수 함수**로 존재하고, `{installed:true, loggedIn:true}`→`사용 가능`+선택 가능, `{true,false}`→`로그인 필요`+선택 불가, `{false, *}`→`미설치`+선택 불가를 반환한다. 파생 입력이 해당 provider 자신의 `ProviderStatus` 1개뿐이다(다른 provider·`deriveConnectionState` 의존 없음) |
| AC-AI9-018 *(v0.0.4 신설)* | REQ-AI9-028, 032 | `installed==false` 또는 `loggedIn==false` 행의 radio가 `disabled`이고 클릭해도 `aiSelectedProvider`가 바뀌지 않는다. 선택 불가 사유 문구가 **그 행 요소 내부**에 존재한다(툴팁·별도 다이얼로그·섹션 각주 아님). `installed==true && loggedIn==false` 행에는 `onStartOnboarding`을 호출하는 컨트롤이 함께 존재한다 |
| AC-AI9-019 *(v0.0.4 신설)* | REQ-AI9-030, 031 | `aria-label="AI 엔진 선택"` 드롭다운이 DOM에 **부재**(행 목록으로 대체, 두 표면 공존 금지). 행 선택 시 `uiStore.aiSelectedProvider`가 `'auto'\|'claude'\|'codex'` 도메인으로 갱신되고 `resolveProviderId` 매핑·`providerId` IPC 계약은 무변경(`src/test/aiProviderId.test.ts` 통과). `aiPolicyStatus().disabled==true`면 모든 행 radio가 `disabled` + 🔒 표기이며 선택 변경 경로가 없다 |
| AC-AI9-020 *(v0.0.4 신설)* | REQ-AI9-033, 034 | `initAiFileSwitchEffects()` 등록 후 `fileStore.currentFile`이 다른 파일로 전이하면 (1) in-flight 요청 `aiCancel` 호출, (2) 활성 고스트 `clearGhostEffect` dispatch, (3) `clearCardRegistry()` 호출이 **각각 1회** 발생한다. 동일 경로 재설정·`null` 유지 시에는 미발동. 정리 로직이 `useFileSystem.openFile`·`uiStore`·`fileStore` 액션 내부에 존재하지 않고, 구독 콜백이 `useFileStore`를 재호출하지 않는다(재진입 없음) |
| AC-AI9-021 *(v0.0.4 신설)* | REQ-AI9-035 | 파일 전환 정리 전후로 `editorStore.content`(및 편집기 `state.doc`)가 **바이트 동일**하다. 이전 문서·새 문서 어느 쪽에도 삽입·삭제·치환이 발생하지 않는다(SPEC-AI-001 REQ-AI-033 / SPEC-AI-005 REQ-AI5-012 무손상 불변) |
| AC-AI9-022 *(v0.0.4 신설)* | REQ-AI9-036, 038 | `error` phase 카드가 `errorKind` `login`/`network`/`other` **3종 전부**에서 `닫기` 컨트롤을 렌더하고, 기존 액션 버튼([연결 안내 보기]/[다시 시도])과 병존한다. 닫기 클릭 시 해당 컨트롤러가 `getCardControllers()`에서 사라지고 문서 텍스트는 무변경이며 재요청·재시도가 발생하지 않는다. `streaming` phase에는 `닫기`가 렌더되지 않는다(기존 [✕ 취소]만) |
| AC-AI9-023 *(v0.0.4 신설)* | REQ-AI9-037 | `empty`·`cancelled-by-new`·`stale` phase 카드도 동일 라벨·동작의 `닫기` 컨트롤을 렌더하고, 클릭 시 레지스트리에서 제거된다. `intruded`/`retry-exhausted`/`diagram-fallback`은 기존 종료 컨트롤을 유지하며 닫기를 중복 추가하지 않는다 |
| AC-AI9-024 *(v0.0.4 신설, 회귀)* | REQ-AI9-033, 036 | **회귀 시나리오** — 파일 A에서 AI 요청이 실패해 `error` 카드가 레지스트리에 남은 상태에서 파일 B를 열면, 카드가 레지스트리에서 사라지고 파일 B 문서 위에 렌더되지 않는다. 파일 B의 본문은 무변경이다. 파일 전환 없이 사용자가 `닫기`를 눌러도 동일하게 사라진다(자동·수동 2경로 모두 성립) |
| AC-AI9-025 *(v0.0.5 신설)* | REQ-AI9-039, 040, 044 | `feature='section-fill'` 요청이 `requestState='done'` + 최종 텍스트 `trim()===''`(빈 문자열·공백만·개행만 각 케이스)로 종결되면, 고스트가 `더 쓸 내용을 찾지 못했어요` 안내와 닫기 컨트롤 **정확히 1개**를 렌더한다. 재요청/재시도 컨트롤은 부재. 반대로 비어있지 않은 done(예: `'내용'`)은 기존대로 고스트 텍스트 + [✓ 넣기]/[✕ 지우기] 2개(+↻)를 렌더한다(두 상태 배타) |
| AC-AI9-026 *(v0.0.5 신설, 회귀)* | REQ-AI9-041 | **회귀 시나리오(재현 파생)** — `startGhostEffect` 후 `useAiStore.getState().completeRequest('', false)` 를 구동하면 (1) `.mdedit-ai-ghost-placeholder` 가 DOM에 **부재**, (2) `넣기` 라벨 컨트롤이 **부재**, (3) 닫기 컨트롤이 **존재**한다. 대기 안내(`WAIT_NOTICE_TEXT`)도 부재. 본 AC는 **현행 구현에서 반드시 실패**한다(현재는 플레이스홀더와 [✓ 넣기]가 동시 렌더됨) |
| AC-AI9-027 *(v0.0.5 신설)* | REQ-AI9-042, 043 | terminal-empty 렌더 전후 및 닫기 클릭 전후로 `view.state.doc.toString()` 이 **바이트 동일**하고, 사용자 조작 없이는 고스트가 자동 소멸하지 않는다. `src/store/aiStore.ts`·`src-tauri/src/ai/codex_cli.rs`·`stream.rs` 의 diff가 0줄이다(빈 done 억제 코드 부재). 기존 고스트 테스트(`aiGhostControls.test.ts`·`aiGhostConfirm.test.ts`·`aiWaitNotice.test.ts`·`aiGhostRerequest.test.ts`·`aiFreeContinue.test.ts`)가 **무수정 전수 통과** |

| AC-AI9-028 *(v0.0.6 신설)* | REQ-AI9-045, 050 | **codex 인자 벡터 스냅샷** — `build_codex_args(Haiku, p, s)` 와 `build_codex_args(Sonnet, p, s)` 를 원소별로 비교하면 길이가 둘 다 15이고 **상이한 인덱스가 정확히 1개**이며, 그 원소가 `model_reasoning_effort="medium"` vs `model_reasoning_effort="high"` 다. `--model` 값은 두 티어 모두 `gpt-5.5`. 격리 플래그 6종(`--ignore-user-config`/`--skip-git-repo-check`/`--ephemeral`/`--sandbox read-only`/`--json`/`-C`)이 두 티어 모두에 존재하고, 기본 티어 벡터는 개정 전 스냅샷과 **바이트 동등** |
| AC-AI9-029 *(v0.0.6 신설)* | REQ-AI9-046, 047, 050 | **claude 사고 예산 티어 분기** — `claude_thinking_env(Haiku) == Some(("MAX_THINKING_TOKENS", "0"))`, `claude_thinking_env(Sonnet) == None`. `spawn_claude` 가 `Some` 일 때만 `.env` 를 호출한다(기본 티어는 설정, 고급 티어는 **미설정**). 소스 전체에서 `MAX_THINKING_TOKENS` 에 결합되는 값 리터럴이 `"0"` 하나뿐이다(숫자 예산 하드코딩 부재). `build_claude_args` 기존 스냅샷 3종(`build_claude_args_haiku_snapshot_byte_equal`·`_sonnet_korean_prompt_snapshot_byte_equal`·`_isolation_flags_never_drop`)이 **무수정 전수 통과** |
| AC-AI9-030 *(v0.0.6 신설, v0.0.7 개정)* | REQ-AI9-048, 050, 053 | **설정 라벨이 백엔드가 보낸 문자열을 렌더** — mock 이 `advancedModelLabel: 'gpt-5.5 · 높은 추론'`(codex) / `'sonnet'`(claude)를 내려주면 선택된 provider 에 따라 라벨이 `고급 모델 사용 (gpt-5.5 · 높은 추론 — 더 정확, 더 느림)` / `고급 모델 사용 (sonnet — 더 정확, 더 느림)` 이고 `aria-label` 도 동일 소스를 쓴다. mock 값을 임의 문자열로 바꾸면 라벨이 **그 값을 그대로 따라간다**(프론트가 재구성하지 않음). 폴백 3경로 — 필드 부재 / 빈·공백 문자열이면 `providerDisplayName` 기반 `{표시명} 고급 티어` 형태, 유효 provider 부재면 중립 문구 — 가 예외 없이 동작하고 `undefined`·`null` 문자열이 노출되지 않는다. 프론트 소스 유래 리터럴 `sonnet` 부재. 토글 체크 상태·`setAiAdvancedModel` 배선·정책 잠금 `disabled`+🔒 무변경 |
| AC-AI9-031 *(v0.0.7 신설)* | REQ-AI9-049, 051, 052 | **라벨과 인자가 동일 함수에서 파생됨(거짓말 불가 구조)** — `ProviderStatus.advanced_model_label` 이 `Option<String>` + `skip_serializing_if` 이고 `AiProviderStatus.advancedModelLabel?: string` 로 대응된다. claude 라벨이 `AiModel::as_arg(AiModel::Sonnet)` 반환값과, codex 라벨이 `codex_model_arg(Sonnet)` 및 `codex_reasoning_effort(Sonnet)` 반환값과 **대조 단언**으로 일치한다(하드코딩 기대 문자열만으로 비교하지 않는다 — 매핑을 바꾸면 테스트가 자동으로 새 값을 요구해야 한다). `codex_effort_display` 는 알 수 없는 키를 원문 그대로 통과시킨다. 라벨 조립부에 `"sonnet"`/`"gpt-5.5"`/`"high"` 리터럴이 없고 동일 문자열의 두 번째 Rust 상수가 존재하지 않는다. 두 어댑터의 `detect()` 가 필드를 `Some(non-empty)` 로 채우고 `detect.rs` 생성 지점은 `None` 이다 |

REQ 커버리지 대조(001–053 + 007a, 013a 전수): 001→AC1·AC2, 002→AC1, 003→AC2, 004→AC3·AC15, 005→AC4, 006→AC5, 007→AC5, **007a→AC5**(spawn 호출 순서), 008→AC5·AC15, 009→AC6, 010→AC6, 011→AC7, 012→AC7, 013→AC8, **013a→AC7**(chunk 1회 emit 후 turn.completed 누락 EOF), 014→AC8, 015→AC9, 016→AC10, 017→AC10, 018→AC3·AC11, 019→AC11, 020→AC11, 021→AC12, 022→AC13, 023→AC13, 024→AC14, 025→AC2, **026→AC16**, **027→AC17**, **028→AC18**, **029→AC16**, **030→AC19**, **031→AC19**, **032→AC18**, **033→AC20·AC24**, **034→AC20**, **035→AC21**, **036→AC22·AC24**, **037→AC23**, **038→AC22**, **039→AC25**, **040→AC25**, **041→AC26**, **042→AC27**, **043→AC27**, **044→AC25**, **045→AC28**, **046→AC29**, **047→AC29**, **048→AC30**, **049→AC30·AC31**, **050→AC28·AC29·AC30**, **051→AC31**, **052→AC31**, **053→AC30**. 총 REQ 55건(001–053 + 007a + 013a), 총 AC 31건(001–031). 미커버 REQ 없음, 고아 AC 없음.

**Quality Gates (AC 외 공통 게이트)**: `cargo test`(codex_cli.rs/detect.rs/stream.rs 단위 테스트 전수 통과) + `cargo clippy` 무경고 + `cargo build` 성공 + 기존 claude 회귀 테스트(claude_cli.rs:309-, stream.rs:128-, detect.rs:290-) 100% 통과(REQ-AI9-022/023/024 가드) + `npm run typecheck` 클린 + `npm run lint` 클린 + `npm test`(Vitest — `SettingsModal.test.tsx`, `aiFileSwitchEffects.test.ts`, `aiSuggestionCard.test.ts`, `aiProviderId.test.ts` + *(v0.0.5)* 고스트 빈 결과 회귀 테스트, `aiGhostControls.test.ts`, `aiGhostConfirm.test.ts`, `aiWaitNotice.test.ts`, `aiGhostRerequest.test.ts`, `aiFreeContinue.test.ts` 포함) 전수 통과 + *(v0.0.6)* `cargo test` 에 codex 티어 인자 벡터 비교(AC-AI9-028)·`claude_thinking_env`(AC-AI9-029) 단위 테스트 포함 및 기존 `build_claude_args_*` 스냅샷 3종 무수정 통과 + `SettingsModal.test.tsx` 라벨 파생 테스트(AC-AI9-030) 통과 + *(v0.0.7)* 라벨-인자 동일 소스 파생 Rust 단위 테스트(AC-AI9-031) 통과 및 `advancedModelLabel` 폴백 3경로 통과 + 수동 통합 테스트(실제 codex CLI 1회 이상 호출, 빈 scratch cwd에서 `agent_message` 1회 도착·`turn.completed` 종료 확인).

## Exclusions (What NOT to Build)

- **`CODEX_HOME` 환경변수 격리 시도 금지** — auth도 `CODEX_HOME`을 써서 인증이 풀림(실측, codex 도움말 명시). 빈 cwd + `--ignore-user-config` + `--sandbox read-only` 조합으로만 격리(REQ-AI9-008).
- **codex 전용 stderr 분류 로직 금지** — 기존 `classify_stderr`(stream.rs:78-126)를 그대로 재사용. login/network/other marker 상수 중복 정의 금지(REQ-AI9-014).
- **설정 다이얼로그 밖 provider 전환 표면 금지** *(v0.0.4 개정 — 0.0.3에서 범위 축소된 REQ-AI9-020 반영)* — 허용되는 유일한 provider 선택 표면은 설정 다이얼로그 `AI 도구` 섹션의 대등 행 목록(REQ-AI9-026~032)이다. provider별 개별 설정 화면·탭, 편집 화면의 상시 "codex 사용 중" 인디케이터, `ai_detect_providers` 결과가 아닌 하드코딩 목록 구동 UI, 섹션 외부의 provider 선택 컨트롤은 도입하지 않는다(REQ-AI9-020). ~~"프론트 provider 선택 UI 전면 금지"(v0.0.1~0.0.2)~~는 폐기됨.
- **파일 전환 정리를 `openFile`/`uiStore`/`fileStore` 내부에 결합 금지** *(v0.0.4 신설)* — 정리는 독립 effects 모듈 + store `subscribe` + `AppLayout` 1회 등록으로만 구현한다(REQ-AI9-034). `useFileSystem.openFile`에 직접 호출을 심으면 워처·복원 경로의 `openFile` 재사용에서 오발동하고, `aiOffEffects.ts`가 문서화한 재진입 없음 속성이 깨진다.
- **정리·닫기 동작의 문서 텍스트 수정 금지** *(v0.0.4 신설)* — 파일 전환 정리(REQ-AI9-035)와 종결 phase 닫기(REQ-AI9-038)는 삽입 전 산출물만 폐기한다. 되돌리기·본문 복원·부분 적용 같은 문서 변경 동작을 함께 수행하지 않는다.
- **`streaming` phase 닫기 중복 추가 금지** *(v0.0.4 신설)* — `streaming`은 이미 [✕ 취소], `intruded`는 [무시], `retry-exhausted`·`diagram-fallback`은 각자 종료 경로를 갖는다. 닫기는 버튼 없는 종결 phase(`error`/`empty`/`cancelled-by-new`/`stale`)에만 추가한다(REQ-AI9-037/038).
- **빈 결과의 백엔드 억제·오류 전환 금지** *(v0.0.5 신설)* — `relay_codex_process` 가 `agent_message` 없이 `turn.completed` 를 만났을 때 `ai://done{result:""}` 대신 emit을 건너뛰거나 `ai://error` 로 바꾸는 수정을 도입하지 않는다(REQ-AI9-043). 빈 결과는 두 provider 모두에서 적법한 종결이며, 표현은 프론트 렌더 계층 단일 지점이 담당한다 — 백엔드에서 갈라놓으면 codex/claude 동작 경로가 분기한다.
- **`aiStore` 리듀서에 빈 결과 분기 추가 금지** *(v0.0.5 신설)* — `reduceCompleteRequest` 의 `streamBuffer = finalText`(권위 값 계약)는 무변경이다. terminal-empty 는 고스트 렌더 계층에서 `status === 'done' && text.trim() === ''` 조합으로 파생한다(REQ-AI9-039, Design Notes).
- **terminal-empty 고스트의 무통보 자동 제거 금지** *(v0.0.5 신설)* — 빈 결과라고 해서 고스트를 사용자 조작 없이 사라지게 하지 않는다. 사용자가 결과를 인지한 뒤 [✕ 닫기]/Esc 로 직접 치우는 것이 유일한 종결 경로다(REQ-AI9-042, 무통보 취소 금지 P7).
- **terminal-empty 안내에 [다시 요청] 추가 금지** *(v0.0.5 신설)* — 고스트 경로는 이미 별도 ↻ 재요청(`reRequestGhost`)을 제공한다. 안내에는 닫기 컨트롤 1개만 둔다(REQ-AI9-044).
- **컨텍스트 문자 상한 변경 금지** *(v0.0.6 신설, 연기 항목)* — 고급 티어라고 해서 `prompt.rs:10-14` 의 `INLINE_SIDE_MAX = 1000` / `SECTION_TAIL_MAX = 1500` / `CONTINUE_HEAD_MAX = 1500` 을 올리지 않는다. 이 값들은 **지연·초점 설계값**이지 모델 컨텍스트 한계가 아니다 — 세 모델(haiku/sonnet/gpt-5.5) 모두 컨텍스트 창이 1500자를 압도하므로 상향에 측정 근거가 없고 지연만 는다. 재검토는 반드시 **`truncated` 플래그의 실제 발생 빈도 계측**에서 시작하며, 그 전에는 상수를 건드리지 않는다(REQ-AI9-024 무변경 가드 연장, Design Notes).
- **고급 티어 사고 예산 숫자 하드코딩 금지** *(v0.0.6 신설)* — 고급 티어에서 `MAX_THINKING_TOKENS` 를 임의의 숫자(4000·8000 등)로 설정하지 않는다. 환경변수를 **설정하지 않아** claude CLI 기본값에 위임한다(REQ-AI9-047). 소스의 유일한 값 리터럴은 기본 티어의 `"0"` 이다.
- **라벨 문자열 복제 금지(프론트 리터럴 + 두 번째 Rust 상수)** *(v0.0.6 신설, v0.0.7 확장)* — 설정 라벨의 모델명·추론 강도 표기는 **백엔드가 중앙 매핑에서 파생해 내려준 `advancedModelLabel` 하나**가 유일한 소스다(REQ-AI9-051/052). `sonnet`·`gpt-5.5`·`high` 를 프론트에 복제하는 것도, 같은 문자열을 다시 적어둔 두 번째 Rust 상수를 두는 것도 금지한다 — 복제본이 생기는 순간 매핑 개정 시 라벨이 조용히 거짓말한다(결함 5의 형태 그 자체). 폴백 시 표시명은 폴백(`?? id`)을 가진 기존 `providerDisplayName` 만 재사용한다(REQ-AI9-049/053).
- **`ProviderStatus`·`AiProviderStatus` 확장은 라벨 필드 1개로 한정** *(v0.0.7 신설)* — 본 개정이 허용하는 IPC 표면 확대는 `advanced_model_label` / `advancedModelLabel` **선택 필드 1개뿐**이다. 기본 티어용 필드·`Capabilities` 노출·provider 별 설명 문구 등 추가 확장은 도입하지 않는다(필요해지면 별도 SPEC). `AiRequestArgs` 와 이벤트 payload 스키마는 여전히 무변경이다(REQ-AI9-021).
- **`AiModel` 부분 개명 금지** *(v0.0.6 신설)* — enum variant 만 티어 중립 이름으로 바꾸고 IPC 문자열 도메인(`'haiku'|'sonnet'`)은 그대로 두는 **부분 개명**을 금지한다. 이름과 값이 어긋나 현행보다 나쁜 상태가 된다. 개명이 필요해지면 IPC 문자열까지 포함한 일괄 개명을 별도 SPEC 으로 다룬다(Design Notes "AiModel 명명 결정").
- **카드 상태 영속화 금지(v1)** — 파일 전환·앱 재시작 후 이전 카드를 복원하는 기능은 도입하지 않는다. 카드는 세션·문서 스코프의 휘발성 산출물이다(REQ-AI9-033의 폐기 계약과 일관).
- **`AiModel` enum 변경 금지** — `Haiku`/`Sonnet` variant 이름 유지. `Codex`/`Gpt55` 등 신규 variant 추가 안 함(REQ-AI9-019).
- **기존 claude 빌드/파싱/조립 로직 직접 수정 금지** — `build_claude_args`, `parse_text_delta`, `parse_final_result`, `ClaudeProvider`, `spawn_claude`, prompt.rs는 무변경(REQ-AI9-022, REQ-AI9-023, REQ-AI9-024, "단일 소스 원칙").
- **codex 모델 다양화 금지(v1)** — `gpt-5.5` 외의 모델(`o1`/`gpt-4o` 등) 선택 옵션은 v1에서 제외. "Model Mapping" 표 개정으로만 도입(REQ-AI9-018).
- **토큰 단위 스트리밍 금지** — codex의 `agent_message`는 완성본 1회 도착(실측). 이를 인위적으로 청킹해 `ai://chunk`를 여러 번 emit하지 않는다(REQ-AI9-011).
- **`~/.codex/AGENTS.md` 오염 사용자 안내 UI 금지** — 문서화(Design Notes)로만 다루고, Run phase에서 별도 차단 로직·사용자 경고 UI를 도입하지 않는다(별도 SPEC).
- **신규 런타임 의존성 금지** — `src-tauri/Cargo.toml` dependencies 무변경. codex는 외부 CLI로 `std::process::Command`로 호출(REQ-AI9-007).
- **nvm 후보·로그인셸 프로브 적용 안 함(codex 한정)** — `resolve_claude_binary`의 nvm 탐지(detect.rs:153-176)·로그인셸 프로브(detect.rs:192-223)는 codex에 v1에서 적용하지 않는다(REQ-AI9-015). 표준 설치 위치 후보만으로 충분.
- **`Capabilities` 소비 분기 신설 안 함** — `supports_streaming`/`typical_latency_ms` 필드는 존재하지만, v1에서는 이를 기반으로 한 런타임 분기를 만들지 않는다(codex도 동일 `relay_process` 패턴을 탄다). 향후 고도화시 별도 REQ.
- **codex 로그인 고도화(토큰 만료 선제 판정) 금지(v1)** — `auth.json` 존재 여부만으로 로그인 판정(REQ-AI9-016). JSON 내용 파싱·만료 검증은 후속 SPEC.
